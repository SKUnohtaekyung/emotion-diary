# 현재 작업

이 파일에는 **진행 중이거나 곧 시작할 작업만** 둔다. 끝났거나 보류된 작업의 절은 [archive/](archive/README.md)로 옮긴다 — 이 파일이 한 번에 읽히는 크기여야 하고, `scripts/claude-scope-guard.mjs`가 이 파일의 `- 소유 파일:` 선언을 전부 합쳐 허용 목록으로 쓰기 때문이다(끝난 작업의 선언이 남아 있으면 그만큼 가드가 헐거워진다). `harness/loop-state.json`의 `task_id`는 아래 절 가운데 진행 중인 것의 머리글과 같아야 한다(`scripts/check-harness.mjs`가 quick에서 확인).

| 작업 | 상태 | 어디에 |
| --- | --- | --- |
| [TASK-WEB-UI-01](archive/TASK-WEB-UI-01.md) 브라우저 우선 UI 프로토타입 | 구현·PC 검수 완료(2026-09-20). PR #30은 CI 두 OS success·병합 가능이나 **2026-09-21 기준 병합 전**(병합은 사용자 지시 때만) | [archive/](archive/README.md) |
| TASK-WEB-UI-02 사용자 QA와 디자인 수정 | in_progress(2026-09-21 착수, 브랜치 `web-ui/qa`) | 이 파일 아래 |
| TASK-HOOK-AUTO auto 모드에서 가드 훅 확인창 없애기 | 구현·검증 완료(2026-09-21, D-060 accepted) — 다른 세션에서의 사용자 확인 대기 | 이 파일 아래 |
| [TASK-MOBILE](TASK-MOBILE.md) 설치형 홈 화면 웹앱 | 보류 — Phase 1 착수 시 재개(이슈 #1~#9) | 별도 파일 |
| [TASK-DS-REF](TASK-DS-REF.md) TDS 규칙 참고 | 보류(이슈 #10~#14, D-039 reserved) | 별도 파일 |
| [TASK-TAXONOMY](TASK-TAXONOMY.md) · [V1](TASK-TAXONOMY-V1.md) · [PLAN-V2](TASK-TAXONOMY-PLAN-V2.md) | 완료(2026-09-05, D-038·D-040 accepted) | 별도 파일(다른 문서가 경로로 인용해 옮기지 않았다) |
| TASK-INFRA-01 · ISSUE-26 · ISSUE-27 · CBM · DESIGN · BOOTSTRAP | 완료·보류 기록 | [archive/](archive/README.md) |

# TASK-HOOK-AUTO — auto 모드에서 가드 훅이 확인창을 띄우지 않게

## 상태

`in_progress`(구현·검증 완료, 다른 세션에서의 사용자 확인만 남음) — 2026-09-21 사용자 지시("자동모드 켜면 수락 버튼 안나오게"), 계획 승인("진행"). 작업 트리는 TASK-WEB-UI-02를 하는 다른 세션과 같이 쓰므로 **이 절의 소유 파일만** 다루고 그 세션의 파일은 건드리지 않는다. 커밋은 사용자가 지시할 때만 한다.

## 목표

권한 모드가 `auto`일 때 `scripts/claude-scope-guard.mjs`·`scripts/claude-git-guard.mjs`가 사용자 확인창을 띄우지 않는다. `auto`가 아닌 모드는 종전대로 확인하되, Claude Code 자신의 작업 폴더(세션 scratchpad·자동 메모리)는 어느 모드에서나 확인 대상이 아니다. 동기는 세션 기록 실측 — 훅이 낸 확인 94건 가운데 77건이 그 작업 폴더 쓰기였고(나머지는 git 16·기타 프로젝트 밖 1) 소유 파일 가드는 `?theme=pebble` 토큰 때문에 29회 크래시했다(설계와 근거는 D-060, `docs/PROCESS_LOG.md` §5).

## 범위

- 포함: 두 가드의 권한 모드 인식(`permission_mode`)·서브에이전트 구분(`agent_id`), 소유 파일 가드의 `?` 크래시 수정과 작업 폴더 제외, 두 테스트 파일 확장(크래시 감지·`?` 회귀·실제 CURRENT_TASK 스모크), AGENTS §2.1·§2.2·DECISIONS·PROCESS_LOG·TRACEABILITY·RISK_REGISTER 동기화.
- 제외: 소유 파일 밖 쓰기의 사후 점검(이슈 #27의 "세션 경계 스냅샷"), Codex 쪽 훅(AGENTS §2.3), 앱 자체가 띄우는 창(브라우저 사이트 허용 등), `bypassPermissions` 모드 처리, `.claude/settings.json` 변경.

## 소유권

- Main/Writer: 이 세션. 하위 에이전트는 실측 한 번(`agent_id` 도착 확인)만 하고 파일을 쓰지 않는다.
- 소유 파일: `scripts/claude-scope-guard.mjs`, `scripts/claude-git-guard.mjs`, `scripts/test-claude-scope-guard.mjs`, `scripts/test-claude-git-guard.mjs`, `AGENTS.md`, `docs/PROCESS_LOG.md`, `docs/DECISIONS.md`, `docs/TRACEABILITY.md`, `docs/RISK_REGISTER.md`

(DECISIONS는 D-060 행, TRACEABILITY는 AG-HARNESS-001 행, RISK_REGISTER는 RK-011 행만 고친다. `tasks/CURRENT_TASK.md`와 `tasks/archive/*`는 TASK-WEB-UI-02 선언에 이미 있다.)

## 인수 조건

- [x] `auto`에서 정상 작업 중 확인창 0회·훅 오류 알림 0회 — 과거 기록 재적용(세션 기록 16개, 모드 auto): Write·Edit 727건 확인 0건·훅 오류 0건, Bash 888건 확인 0건(수정 전 기록: 확인 94건·오류 29건). 이 세션(auto)에서는 수정 뒤 Write·Edit·Bash 42건에 훅의 ask·오류 기록이 0건이고, 예전에 창이 뜨던 종류인 scratchpad 쓰기를 직접 해 그대로 통과했다. 하위 에이전트의 커밋·푸시는 예외로 확인을 유지한다(파이프 테스트, 실측한 `agent_id`).
- [x] `auto`가 아니면 scratchpad·자동 메모리 밖의 소유 밖 쓰기와 커밋·푸시는 종전대로 확인하고, 모드를 알 수 없으면(`permission_mode` 없음) 안전 쪽(확인)으로 동작한다 — 파이프 테스트(default·acceptEdits·plan, 모드 필드 없음).
- [x] 두 가드 테스트가 새 동작(모드·작업 폴더·서브에이전트)과 `?` 회귀, 훅 종료 코드 검사, 실제 `tasks/CURRENT_TASK.md`로 한 번 돌리는 스모크를 담고 통과한다 — 소유 파일 가드 30건·git 가드 25건. 수정 전 가드(HEAD)에서는 16건·5건이 실패해 회귀 감지력을 확인했다.
- [ ] `node scripts/verify.mjs --mode quick`·`--mode full` PASS(완료, exit 0)와 이 세션에서 확인창이 사라진 것(완료). **남음: 다른 세션(`6c7f82b8`)에서 사용자가 확인창이 뜨지 않는지 본다** — 그 세션은 지금 쉬고 있어 새 기록이 없고, 훅은 호출마다 새로 실행되므로 그 세션의 다음 쓰기부터 적용된다.
- [x] AGENTS §2.1·§2.2, D-060, PROCESS_LOG §5, RK-011, AG-HARNESS-001을 코드와 맞췄다. 사용자 확인 뒤 이 절을 `tasks/archive/TASK-HOOK-AUTO.md`로 옮긴다(끝난 작업의 선언이 남으면 가드가 헐거워진다).

## 체크포인트

- 착수(2026-09-21): 원인 진단은 세션 기록 재적용(확인 94건 = scratchpad 47·자동 메모리 30·기타 프로젝트 밖 1·git 16, 프로젝트 안 0건 / 훅 크래시 29회)과 스크린샷 재현으로 끝났다. 공식 문서상 훅 입력에 `permission_mode`·`scratchpad_dir`·`transcript_path`가 있고 서브에이전트 호출에는 `agent_id`가 붙는다 — 이 앱 런타임에서 실제로 오는지는 아래 "실측 완료"에서 확인했다.
- 실측 완료(2026-09-21): 가드에 임시 프로브를 넣어 이 앱(Claude Code 2.1.275)의 PreToolUse 입력을 기록했다. 주 에이전트 호출은 `permission_mode: "auto"`·`scratchpad_dir`(짧은 이름 표기 NOHTAE~1)·`transcript_path`(긴 이름 표기)가 오고, 하위 에이전트(`verifier`)의 Bash 호출에는 `agent_id`·`agent_type`이 붙으며 `permission_mode`는 auto를 이어받는다. 프로브는 지웠고 `PROBE-TEMP` 잔존은 0건이다.
- 구현 완료(2026-09-21): 소유 파일 가드 — 모드 인식, 작업 폴더 제외(실경로로 맞춰 짧은·긴 이름 혼용 대응), `?`를 글자로 취급, 패턴 하나가 깨져도 훅이 죽지 않게 try/catch. git 가드 — auto의 주 에이전트만 통과하고 하위 에이전트는 사유를 바꿔 확인. 테스트는 소유 파일 가드 11→30건, git 가드 15→25건. 모든 케이스가 훅 종료 코드 0을 요구한다(죽은 훅은 stdout이 비어 pass로 보인다).
- 검증(2026-09-21): 위 인수 조건의 수치. `node scripts/verify.mjs --mode quick`·`--mode full` PASS(exit 0, `npm run test` 안에서 가드 테스트 30건·25건 포함). 과거 호출 재적용에서 default 모드를 넣으면 프로젝트 안 소유 밖 218건·기타 프로젝트 밖 3건·git 52건이 확인으로 남는다 — 설계대로이며, 옛 훅은 그 자리에서 크래시해 조용히 통과시켰다.
- 함정(다음 세션용): 도구 호출 매개변수에서 백슬래시 두 개가 하나로 줄어든다(Bash heredoc로 쓴 스크립트의 정규식 리터럴이 구문 오류가 됐다). 정규식·경로 이스케이프는 그 줄을 건드리지 않고 좁게 고치거나 `String.fromCharCode(92)`를 쓴다. 같은 파일에 편집을 동시에 보내지 않는다.
- 남은 것: ① 다른 세션에서 확인창이 안 뜨는지 사용자 확인 → 확인되면 이 절을 `tasks/archive/TASK-HOOK-AUTO.md`로 옮기고 위 표를 갱신 ② (이번 범위 밖) 소유 파일 밖 쓰기의 사후 점검 — 이슈 #27의 세션 경계 스냅샷 ③ `bypassPermissions`를 쓰게 되면 같은 처리를 할지 결정 ④ 결정 번호 D-060은 이 작업이 썼다 — TASK-WEB-UI-02의 다음 신규 번호는 D-061이다.

# TASK-WEB-UI-02 — 사용자 QA와 디자인 수정

## 상태

`in_progress` — 2026-09-20 사용자 지시("다음 작업은 내가 QA 및 디자인 수정할 거야"), 2026-09-21 착수. **사용자가 눈으로 보고 판단하는 사람이고, 에이전트는 그 판단을 받아 고치고 다시 보여 주는 손이다.** 사용자는 개발 용어를 모른다 — 고칠 곳은 화면을 보며 일상어로 말하고, 에이전트가 그것을 파일·값으로 옮긴다.

## 목표

사용자가 `web/` 화면 시안을 직접 눌러 보며 찾은 어색한 점을 고치고, 그 과정에서 확정된 모양을 디자인 시스템(문서·값·전시장)에 되돌려 기록한다.

## 범위

- 포함: 사용자 QA에서 나온 시안 수정(배치·문구·크기·둥글기·색 쓰임), TASK-WEB-UI-01이 남긴 미검증 항목의 사용자 확인, 시안을 만들며 규칙 없이 임시로 정한 부품의 규칙화(아래 "임시 부품 목록"), 값 변경 시 `tokens.json`·`DESIGN_SYSTEM.md`·`style-guide.html` 동시 갱신.
- 제외: 저장·로그인·AI·분석·내보내기·삭제 구현, UI 프레임워크·부품 코드 묶음(Phase 1 결정, D-047), 다크 모드 적용(D-034), 로고(D-036), 감정 계열 색의 의미 변경(PR-010 — 사용자 승인 없이는 금지), 캐릭터 그림 재생성.
- 부품 규칙을 크게 보강하게 되면 보류 중인 [TASK-DS-REF](TASK-DS-REF.md)(D-039 reserved)를 재개할지 사용자에게 먼저 묻는다. 이 작업이 그 결정을 은근히 대신하지 않는다.

## 소유권

- Main/Writer: 착수하는 세션 1명. 하위 에이전트는 읽기 전용이 원칙이다. **예외(2026-09-22 사용자 지시 "병렬로 처리할 수 있는 작업들은 agent 실행시켜서 병렬로 작업해", "걍 sonnet으로 해")**: 파일 소유를 겹치지 않게 나눈 Sonnet 작업자 여럿이 같은 작업 트리에서 동시에 쓴다(별도 worktree는 쓰지 않는다 — 커밋되지 않은 v4 작업이 worktree에 없다). 작업자는 커밋·푸시·`git add`를 하지 않고 소유 밖 파일을 고치지 않으며 필요한 패치는 보고서로 돌려준다. 통합·검증은 Main이 한다.
- 소유 파일: `web/`, `scripts/web-preview.mjs`, `design/tokens.json`, `design/style-guide.html`, `docs/DESIGN_SYSTEM.md`, `docs/UX_SPEC.md`, `docs/DECISIONS.md`, `docs/STATUS.md`, `tasks/NAV-TODAY-NOTES.md`(2026-09-24 추가 — 오늘 화면 하단 탐색 논의 노트), `harness/loop-state.json`, `tasks/CURRENT_TASK.md`, `tasks/archive/*`, `design/brand-concepts.html`(2026-09-21 사용자 요청으로 추가 — 브랜드 무드 탐색 페이지), `design/brand-theme-pebble.json`(같은 날 추가 — 조약돌 예시의 후보 값, 시안이 `?theme=pebble`일 때만 읽는다), `design/pebbles/*`(같은 날 사용자 확정 조약돌 원본 9종과 매핑 기록), `references/source/pebbles/*`(같은 원본 9종 보존), `design/characters/ui-poses/*`, `design/characters/flat-friends/*`, `references/source/characters/flat-friends/*`, `design/characters/README.md`(같은 날 추가 — 사용자가 지적한 UI 포즈 그림의 이웃 조각 제거와 확정 평면 친구 이름·작명 이유), `design/character-compare.html`(같은 날 추가 — 사용자가 시킨 캐릭터 벡터 보기 임시 화면, `web/`는 바꾸지 않는다), `design/temp-flat/*`(같은 날 추가 — 예시 시트에서 오린 임시 그림과 현재 시안 캡처, 확정되면 정리한다), **브랜드·디자인 시스템 개편 몫(2026-09-21 사용자 지시로 추가)**: `docs/BRAND_STORY.md`·`docs/DESIGN_RATIONALE.md`(새 정본), `design/fonts/*`(Pretendard 가변 폰트 사본과 라이선스, 사용자가 다운로드를 허락함), `design/pebbles/ui/*`(조약돌 원본에서 줄인 화면용 파생본 320·128px), `design/characters/flat-friends/ui/*`(평면 친구 원본에서 줄인 화면용 파생본 480·160px — 원본은 다른 세션이 D-051로 확정한 것이라 손대지 않는다), `PRD.md`(2026-09-24 사용자 지시로 전체 — 결정·화면과 어긋난 곳 동기화), `docs/DATA_MODEL.md`·`docs/ARCHITECTURE.md`(같은 날 — 하루 기준 시각·지난 날 새 기록 금지·세션 임시 보관의 계약 영향만), `README.md`(정본 지도 행만), `docs/TRACEABILITY.md`·`docs/RISK_REGISTER.md`·`docs/EVAL_PLAN.md`(개편으로 영향받는 행만), `scripts/check-contrast.mjs`(서비스 색·감정 테마 검사)., `docs/DATA_MODEL.md`(2026-09-22 친구와의 친밀도 정의를 §7에 추가하려고 포함)

## 착수 절차

1. `main`이 TASK-WEB-UI-01 PR을 포함하는지 확인한다(`web/index.html` 존재). 새 브랜치(예: `web-ui/qa`)를 만든다.
2. TASK-WEB-UI-01 절을 `tasks/archive/TASK-WEB-UI-01.md`로 옮기고(규칙 `archive/README.md` — 본문 바이트 그대로, 옮긴 뒤 원문과 대조. 작업 트리는 CRLF·blob은 LF라 줄 끝을 맞춘 뒤 비교한다) `harness/loop-state.json`의 `task_id`를 `TASK-WEB-UI-02`로 바꾼다. 둘이 어긋나면 quick이 실패한다.
3. 미리보기 두 개를 연다: `web-prototype`(4174, 시안)과 `design-preview`(4173, 부품 전시장). 서버는 Bash가 아니라 `preview_start`로 띄운다. 사용자에게는 Viewport 메뉴의 Mobile을 안내한다.
4. 사용자에게 QA 방법을 제안하고(화면별로 같이 보기 / 사용자가 먼저 둘러보고 한꺼번에 말하기) 고른 방식으로 진행한다.

## 고치는 순환(한 건마다)

1. 사용자가 말한 것을 "어느 화면의 무엇이 어떻게 보이면 좋겠다"로 되물어 확인한다. 모양 선택지는 말이 아니라 **시안에서 바꿔 보인 화면**으로 비교하게 한다.
2. 바꾸기 전 화면을 캡처하고, 가장 작은 변경을 넣고, 같은 자리의 바꾼 뒤 화면을 보여 준다.
3. 값(색·글자·간격·둥글기)이면 `design/tokens.json`을 고친다 — 시안은 실행 중에 읽으므로 바로 반영된다. 같은 변경에서 `docs/DESIGN_SYSTEM.md`와 `design/style-guide.html`의 `:root`도 고친다(어긋나면 quick 실패). 색을 바꿨으면 `node scripts/check-contrast.mjs --verbose`.
4. 모양 규칙이면 `web/css/`의 해당 파일(아래 "어디를 고치면 무엇이 바뀌나" 표)을 고치고, 확정되면 `DESIGN_SYSTEM.md` §6에 부품 규칙으로 적는다. 기존 결정(D-033~D-037, D-041, D-047)을 뒤집는 변경은 `DECISIONS.md`에 새 행을 먼저 남긴다(다음 번호는 그 파일의 D-039 행에서 확인).
5. 360px에서 가로 넘침 0과 조작 요소 44px를 다시 잰다. 측정 방법은 `tasks/archive/TASK-WEB-UI-01.md`의 "검수 증거".

## 어디를 고치면 무엇이 바뀌나

(2026-09-25 현재 `web/` 파일 지도. `css/app.css`·`sky.js`·`picker.js`·`record-card.js`와 `?theme=pebble` 예시 파일은 무드 v4 재구성에서 사라졌다.)

| 바꾸고 싶은 것 | 고칠 곳 |
| --- | --- |
| 색, 글자 크기, 간격, 모서리 둥글기(버튼 8·카드 12·시트 16·알약 999), 최소 터치 크기 | `design/tokens.json`(+ `DESIGN_SYSTEM.md`, `style-guide.html`) — `web/js/tokens.js`가 CSS 변수로 옮긴다 |
| 공통 부품(버튼·입력·시트·하단 탐색·오류 문구)과 기본 간격 | `web/css/base.css` |
| 오늘 화면의 숲·회색 길·조약돌 아홉·큰 질문 | `web/css/scene.css`, `web/js/components/forest.js`(숲·식물·조약돌 자리), `web/js/views/today.js`(상태·돌 클릭) |
| 마음 고르기(초록 언덕 자유 배치) | `web/js/components/meadow.js`, `web/css/write.css`(`.pick-*`, `.ff`) — 이전 격자는 `friends.js`(`#/write?pick=grid`) |
| 작성 단계의 순서·제목·안내 문구, 단계 상단 바·하단 버튼 줄 | `web/js/views/write.js`, `web/css/write.css` |
| 세부 감정 화면(연한 배경·pill·바구니) | `web/js/components/detail.js`, `pile.js`(바구니와 담기), `web/css/basket.css`, `web/css/write.css`(`.detail-screen`), 색 섞기는 `web/js/dom.js`의 `setTheme` |
| 크기 화면('길 위의 친구') | `web/js/components/slider.js`, `web/css/size.css` |
| 이유·오늘 있었던 일·칭찬·감사 화면 장면 | `web/js/components/reason.js`+`css/reason.css`, `components/notes.js`+`css/notes.css`(입력 묶음은 `views/write.js`가 만들고 장면이 감싼다) |
| 이대로 남길까요(편지)와 기록 상세 읽기 모드 | `web/js/components/letter.js`, `web/css/letter-scene.css`(장면·정중앙·스크롤바), `web/css/write.css`(카드 기본) |
| 완료·온보딩의 언덕과 친구 배치, 큰 글자 격자 | `web/js/components/land.js`(자리표·`watchFreeGrid`), `web/css/land.css`(언덕 기본 규칙·`.land-fit`·격자 — 2026-09-25에 scene.css·tabs.css에서 모음), 이유 화면은 `components/reason.js`+`css/reason.css` |
| 달력·설정 화면의 구성과 문구 | `web/js/views/tabs.js`, `web/css/tabs.css`, `web/css/tabs-mood.css`(큰 달 제목·달력 미리보기 종이·펼침, D-088) |
| 통계(크기 추세·친구 걸음·불러오기 상태) | `web/js/views/stats.js`, `web/js/stats-calc.js`(계산, D-086), `web/css/stats.css`(`sx-` 접두어) |
| 작성 흐름 예외(크기 시트·완료 버튼·저장 안 됨·뒤로 가기) | `web/js/views/write.js`, `web/js/state.js`, `web/css/flow.css`(`wf-` 접두어) |
| 이어지는 전환(단계 전환·봉인·달력 조약돌 배지) | `web/css/film.css`, `web/js/views/write.js`(`playSealSequence`·`playCalendarPebble`), `web/js/main.js`(작성 단계 사이만 View Transitions, D-089) |
| 온보딩·위기 안내 | `web/js/views/entry.js` |
| 예시 기록·상태(QA 주소 `?s=` 등) | `web/js/sample.js`, `web/js/main.js`(라우터) |
| 감정 단어 자체 | 시안이 아니라 `data/taxonomy/v2.json`(검수된 정본 — 이 작업 범위 밖) |

## 사용자 QA 점검표(TASK-WEB-UI-01이 확인하지 못한 것부터)

- [ ] 키보드: Tab으로 "다음"까지 간 뒤 Enter, Space로 눌린다(자동 도구로는 확인 불가였다).
- [ ] PC의 "움직임 줄이기"(Windows 설정 → 접근성 → 시각 효과 → 애니메이션 효과 끔)를 켜면 고른 친구가 움직이지 않는 그림으로 나온다.
- [ ] 회색조로 봐도(또는 이름표를 가리고 봐도) 9종 캐릭터와 고른 점이 구별된다.
- [ ] 분노·미움·공포·혐오 캐릭터가 무섭거나 조롱하는 것처럼 보이지 않는다.
- [ ] 초성 줄(오른쪽 ㄱㄴㄷ)이 작아서 누르기 어려운가 — 32×24.6px, 제품 기준 44px 미달. 없애기/넓히기/그대로 가운데 정한다.
- [ ] 단계형과 긴 한 장 가운데 실제로 쓰고 싶은 쪽, 둘 다 남길지.
- [ ] 화면별 인상: 오늘(시작) / 날짜·사건 / 감정 계열 / 세부 감정 / 강도 / 이유 / 칭찬·감사 / 검토 / 완료 / 달력 / 통계 / 설정.
- [ ] (기기가 있을 때) iPhone Safari·Android Chrome에서 같은 흐름 — 같은 와이파이로 열려면 미리보기 서버를 집 안 네트워크에 여는 변경이 필요하고, 이는 사용자 승인 뒤에만 한다.

## 임시 부품 목록(규칙 없이 시안에서 정한 것 — 규칙화 후보)

두 칸 전환 버튼(`.segmented`), 진행 막대(`.progress`), 스위치(`.switch`), 작은 표지(`.proto-tag`·`.badge`), 묶음 상자(`.panel`)와 빈 상태 상자(`.empty-box`), 글자 링크 버튼(`.link`), 입력 오류 문구(`.field-error`), 아래 고정 버튼 줄(`.step-footer`), 지우기 달린 알약(`.pill`). 버튼의 눌림·hover·처리 중 상태는 `DESIGN_SYSTEM.md` §6.1에 한 줄씩만 있고 견본이 없다.

## 에이전트 자체 점검(2026-09-21 — 사용자 지적 전, 시안은 바꾸지 않았다)

360×740 캡처 20여 장(오늘 · 작성 7단계 · 완료 · 완료 뒤 오늘 · 달력 · 통계 · 설정 · 긴 한 장)과 측정 스크립트로 찾았다. **결정이 아니라 후보**이며, 사용자 목록을 받으면 "겹친 것 / 사용자만 찾은 것 / 에이전트만 찾은 것"으로 대조해 보고한다. 성격: **결함**=수치로 확인된 어긋남, **판단**=위계·리듬에 대한 디자인 판단(사용자 취향이 갈릴 수 있음).

| # | 화면 | 찾은 것 | 성격 | 고칠 곳 후보 |
| --- | --- | --- | --- | --- |
| 1 | 오늘·통계(빈 상태)·완료 | UI 포즈 그림 4종의 가장자리에 이웃 동물 조각이 남아 있다 — 거북이 옆 주황 조각이 화면에 그대로 보인다. `ui-poses/*-120.png` 가장자리 2px 안의 불투명 픽셀: 희망(거북이) 오른쪽 90, 슬픔(기니피그) 왼쪽 65, 즐거움(양) 오른쪽 25, 공포(토끼) 오른쪽 16, 나머지 5종 0 | 결함 | 그림 파일(재생성이 아니라 조각 지우기지만 "캐릭터 그림" 범위 밖 항목이라 사용자 결정) → **2026-09-21 사용자 지시로 반영**(QA 처리 기록 1) |
| 2 | 세부 감정 | 감정 줄이 화면에 2.5줄뿐이다: 보이는 목록 274px 중 그룹 머리글·"최근 사용" 자리(51px)를 빼면 줄 영역 약 160px. 목록 위 부속물이 337px(시안 띠·전환 버튼·단계 표시·제목·안내·검색·빈 트레이)이고, 3개만 골라도 알약이 2줄(약 96px)이 돼 목록은 1.5줄로 준다. 목록 아래 46px와 초성 줄 아래쪽(ㅍ·ㅎ)이 하단 버튼 줄에 가려진다(목록 337~657, 버튼 줄 시작 611) | 결함 | `write.js`, `app.css`(`.list` 높이) |
| 3 | 작성 1→2단계 | 하단 버튼 줄이 단계마다 달라진다: "다음"의 왼쪽 위치·너비가 1단계 113.7·229.5px, 2~7단계 86.1·257.1px("그만두기" 88.9px, "이전" 61.3px) | 결함 | `write.js`, `app.css` |
| 4 | 감정 계열 | 별자리 라벨 슬픔·공포는 자기 점(32px)보다 이웃 점(29px)이 가깝고 희망·혐오는 4px 차다. 라벨 글자를 눌러도 반응이 없다(라벨이 44px 누름 영역 밖이고 `pointer-events:none`). 프레임 320px가 본문 열 326.4px보다 좁아 제목·안내·진행 막대와 좌우 3.2px씩 가장자리가 안 맞는다 | 결함 | `sky.js`, `app.css` (라벨 위치 규칙은 DESIGN_SYSTEM §6.2·D-041이라 바꾸면 결정 행이 먼저) |
| 5 | 날짜와 사건 | 도움말 위치가 다르다 — 날짜는 입력 아래, 사건은 입력 위 | 결함 | `write.js` |
| 6 | 검토·완료 | 칭찬·감사 칸이 입력 칸과 같은 테두리 상자라 눌러서 고치는 칸처럼 보이고, 빈 칸도 "칭찬 3" 회색 자리표시가 그대로 나온다 | 결함 | `record-card.js`, `app.css`(`.slot`) |
| 7 | 색(전 화면) | 카드(`#FAFAF8`) 안 12px 라벨 — `text-subtle` `#767570` 대비 4.42:1로 AA 4.5에 0.08 모자란다. 입력·칸 테두리(`border-strong` `#C9C7BF`)는 흰 바탕에서 1.69:1로 비텍스트 3:1 권고에 못 미친다("얇은 선" 방향이라 사용자 결정) | 결함 | `design/tokens.json` (+ DESIGN_SYSTEM·style-guide 동시) |
| 8 | 통계·설정 | 어색한 줄바꿈 — 통계 카드 "최근 7일 · 기록 / 5개"(한 어절만 아래로), 설정 "…분석·감시되지 / 않습니다." | 결함 | `tabs.js`, `app.css` |
| 9 | 작성 단계 전반 | 내용이 시작되기 전 약 170px(시안 띠 34 + 전환 버튼 56 + 단계 표시·막대 + 제목)를 쓰고, 전환 버튼의 검은 채움이 본 내용보다 강하다. 아래도 단계 버튼 줄 73 + 하단 탐색 57 = 130px(화면의 17.5%)가 고정 | 판단 | `write.js`, `app.css` |
| 10 | 강도 | 값 없음 표시 "–"가 빼기 버튼 "−"과 같은 모양이고, 값 없음일 때는 손잡이가 없어 어디를 눌러야 하는지 단서가 약하다 | 판단 | `slider.js`, `app.css` |
| 11 | 한 칸 단계 | 제목과 질문이 겹친다 — "이유" 아래 "왜 이런 감정이 들었다고 느끼나요?", "날짜와 사건" 아래 "날짜" | 판단 | `write.js` |
| 12 | 오늘 | 핵심 버튼이 화면 가운데(y≈364/740)에 있고 아래 약 210px가 비어 엄지가 닿는 아래쪽을 쓰지 않는다. 캐릭터 9종은 5+4로 줄바꿈되며 가운데 정렬이고 글은 왼쪽 정렬이다 | 판단 | `tabs.js`, `app.css` |
| 13 | 달력 | 이전·다음 달 이동이 없다. 칸 사이 간격이 가로 0·세로 4px로 달라 세로 이음선이 두 칸 테두리가 겹쳐 2px로 보인다. 상태 표시(✓·✎)가 10px | 판단 | `tabs.js`, `app.css` |
| 14 | 글(전 화면) | "작성 streak"(통계 카드), "작성 streak를 되돌려…"(날짜 안내)처럼 영어가 한글 화면에 그대로 있다 | 판단 | `tabs.js`, `write.js` |

지켜야 할 것(잘 된 것): 강도 슬라이더의 채움·손잡이·눈금과 큰 숫자, 선택 행의 체크 원과 계열 색조, 색 알약, 하단 탐색의 선/면 아이콘, 통계 막대의 색+수치 병기.

**사용자 지적과의 대조(2026-09-21)** — 겹친 것: #1(그림 조각)·#12(오늘이 허전함)·#13(달력 상태 구분). 사용자만 찾은 것: 브라우저 날짜 선택창의 이질감, 서비스 고유색, "감정 그 자체" 주인공 캐릭터, 그리고 무엇보다 **브랜드 무드·컨셉을 먼저 정하자는 방향**. 에이전트만 찾은 것(사용자 확인 전 — 브랜드 방향이 정해진 뒤 QA를 이어 갈 때 다시 꺼낸다): #2~#11, #14.

## QA 처리 기록(사용자가 말한 것 → 결과)

한 건마다 적는다: 화면 · 사용자 말 → 확인한 뜻 · 전/후 · 처리(반영·보류·기각과 이유). 사용자 말은 음성 받아쓰기라 뜻이 겹치는 곳이 있어 항목으로 나눠 적었다.

| # | 화면 | 사용자 말(2026-09-21) → 확인한 뜻 | 처리 |
| --- | --- | --- | --- |
| 1 | 오늘·통계(빈 상태)·완료 | "거북이 이미지가 햄스터 엉덩이까지 포함돼 있다" → 거북이 오른쪽에 기니피그 뒤태 조각이 걸려 있다(자체 점검 #1과 같은 결함) | **반영.** 전: 거북이 오른쪽 가장자리에 조각 25,390px, 기니피그 왼쪽 위에 노란 조각 3,395px. 후: 두 조각과 둘레만 투명하게(1024 원본 29,378·5,501px, 120px 파일 407·86px — 바뀐 것은 전부 "지워짐"뿐), 다시 분석하면 이웃 조각 0개, `check-characters` PASS. 오늘 화면 후 캡처 확인. 즐거움(양) 귀 끝·공포(토끼) 잎 하나는 이웃 조각이 아니라 그림 자신이 살짝 잘린 것이라 두었고 캐릭터 README에 적었다 |
| 2 | 오늘 | "오늘에 들어가는 요소가 너무 적다. 직접 작성·AI 대화 말고도 더 많은 게 있으면" → 오늘 탭이 캐릭터·글·버튼 두 개뿐이라 허전하다 | **보류(브랜드 방향 뒤).** 후보 3개를 세 안 모두에 넣어 보임 — 이번 주 발자국(7일 상태 줄), 오늘의 한 줄 질문, 일주일 전 오늘 |
| 3 | 오늘 | "요소들이 이열로(일렬로) 가지런한데, 하나의 '나의 노트'·'나만의 …' 느낌이 나게 자유분방하게" → 캐릭터를 줄·격자가 아니라 흩어 놓고 화면 전체를 한 권의 노트처럼 | **보류(브랜드 방향 뒤).** 안 2(스티커 흩어놓기)·안 3(풍경 속 깊이 배치)로 보임 |
| 4 | 오늘 | "자연 분위기를 살리고, 컨셉을 잡자 — 숲속을 탐험하는 나" | **브랜드 무드 탐색으로 전환**(사용자 지시). `design/brand-concepts.html`(전시장 4173) 세 안, 답 대기 |
| 5 | 오늘 | "버튼은 버튼으로 유지하되 배경을 깔던가" → 버튼 모양은 두고 뒤에 바탕을 깐다 | **보류.** 안 2(종이 태그+테이프)·안 3(나무 팻말)로 보임 |
| 6 | 오늘 | "감정이라는 것 자체를 나타내는 동물이나 사람이 있었으면" → 서비스 주인공 캐릭터 | **보류.** 후보 3갈래(탐험가 사람 / 다람쥐 안내자 / 마음 도토리). 새 그림이 필요한데 에이전트는 그림을 그릴 수 없어 Codex 이미지 생성이 필요하다 — 결정 D-036(로고·워드마크 없음, 새로 그리지 않음)과 범위 제외(캐릭터 그림 재생성)를 사용자 결정으로 바꿔야 한다 |
| 7 | 달력 | "연필·체크·기록 없음의 구분이 안 된다. 컬러로 확실하게, 다만 감정 9색은 빼고 서비스 색으로" → 상태 3종을 서비스 색으로 | **보류(색 결정 뒤).** 시안: 진한 채움+발자국/체크 · 연한 채움+점선+연필 · 빈 점선(진하기 3단계+모양) |
| 8 | 작성 1단계 | "날짜를 누르면 나오는 선택창이 이질적이다" → 브라우저 기본 날짜 선택창이 앱과 따로 논다 | **보류(브랜드 뒤).** 방향: 달력 화면의 달력 부품을 그대로 쓰는 앱 자체 날짜 시트 |
| 9 | 통계 | "괜찮긴 하다" | 유지 |
| 10 | 전체 | "QA를 하기 전에 브랜드 무드부터, QA보단 컨셉을" | **진행 중** — 세 안과 제 제안을 보이고 사용자 선택을 기다린다 |
| 11 | 브랜드 | 사용자가 외부 대안 "마음의 숲 탐험 노트"(반딧불이 안내자 + 기존 9종 유지 + 숲 공터 + 달력 UI화, 그림 2장: 반딧불이·새 9종 시트)를 주며 "프로젝트 존재 이유와 심리학적으로 냉철하게 판단"을 요청 | **평가함(결정 아님).** 기준은 PRD §2(정확한 말·기록 습관·개인 기록 안의 변화 성찰)와 PR-001·PR-010·D-020·SAFETY §6. **채택**: 숲/노트 컨셉, 느슨한 배치(크기·위치는 감정과 무관하게 고정), 44px, 달력·날짜를 UI로. **수정 후 보류**: 반딧불이 — 측정으로 노란 불빛 `#FDE677`이 기쁨300과 ΔE 2.2(사실상 같은 색), 연두 날개·더듬이가 즐거움과 7~8, 몸통 짙은 초록만 11.6 이상 떨어짐. 늘 웃는 얼굴, '안내자' 서사가 사용자의 주체성을 가져감, Phase 3에서 AI의 얼굴이 됨(직접 작성은 내용을 읽지 않는데 표정이 있는 존재가 있게 됨), 밝은 화면(D-034)에서 "어둠 속 빛" 정체성 상실. 대안: "내가 든 불빛(도구)"·얼굴 최소·크림빛, 또는 별자리 점을 반딧불로 재해석(새 캐릭터 0). **반대**: 새 9종 플랫 시트 — 몸 색이 9/9 자기 감정 토큰에 가장 가깝고(혐오 ΔE 2.1), 정면 전형 표정이라 D-044의 자세 근거를 버리고, D-045(크레용 외곽선·종이결)를 번복하며 현 캐릭터 파일 95개를 무효로 하고, 어린이 앱 어법. **순서**: UI(서비스 색·달력 3단계·날짜 시트·오늘 구성)+기존 9종 먼저, 안내자·풍경 그림은 2주 써 본 뒤 |
| 12 | 브랜드 스토리 | 사용자가 브랜드 의미를 정해 줌 — 반딧불이의 밝은 빛=행복, 짙은 청록 `#09232?`(여섯째 자리 미기재)=안정·깊이·신뢰와 절제·자연성·내면과 사색, 자유와 관련된 색도 쓰고 싶다. 행복(즐거움·만족·의미·관계·성장)·자유(선택권·자율성·행동 가능성·억압으로부터의 해방)의 정의와 강연 메모("자유=나로부터 시작되는 삶, 핵심은 환경이 아니라 자기 자신")를 주며 "브랜드 스토리를 어떻게 잡을지" 요청 | **제안함(결정 대기).** `design/brand-concepts.html`에 안 4 "밤의 숲, 작은 빛"(#b-t4)을 추가. 측정: `#09232?`는 여섯째 자리 16가지 모두 L*≈12, 흰 글자 대비 16:1 이상, 가장 가까운 감정 색(공포900·슬픔900)과 ΔE 10.4~14.3으로 기준 7을 통과(짙은 청록은 공포 계열과 색이 이웃이라 공포 화면에서 정체성 주의). 자유의 하늘색은 옅은 톤이 슬픔 50·100과 ΔE 2.5~4.0이라 감정 색과 구별되지 않고(맑은 하늘 `#5FA8D3`만 8.6) → 자유는 색이 아니라 여백·길(공간)로 표현. 빛은 노랑(기쁨300과 ΔE 2.2) 대신 색 없는 크림빛(`#FFF3D6`)으로. 스토리 뼈대: 알아차림(제품이 하는 일) → 자유(나로부터 고르는 것 — "내가 선택한 표현", "다른 표현도 괜찮아요", 내보내기·삭제) → 행복(의미·감사, 약속하지 않음). "빛=행복"은 조건 3개(색 없는 빛 / 사용자가 쓴 알아차림·칭찬·감사에만 켜짐 / 빛이 없는 날도 온전)를 지킬 때만 |
| 13 | 브랜드 은유 | 사용자가 "숲 속에서 빛을 밝혀 조약돌을 줍는 느낌"을 제안하고 생각해 보라고 함 | **평가·시안(결정 대기).** `design/brand-concepts.html`에 안 5 "시냇가의 조약돌"(#b-t5)을 추가. 판단: **강한 채택 권장.** ① 빛을 켜고 줍는 쪽이 사용자 자신이라 안내자의 주체성 문제가 풀림 ② 얼굴 없는 중립 사물이라 판정·페르소나·AI 얼굴 문제가 없음 ③ 조약돌 하나=세부 감정 하나(PRD §6.2의 저장 단위)로 1:1 ④ 새 캐릭터 그림이 필요 없음(코드로 그린 도형) — "감정 그 자체" 주인공 문제와 D-036(새로 그리지 않음) 우려가 함께 풀림 ⑤ 산길 돌탑·서낭당 돌무더기·헨젤과 그레텔의 달빛 조약돌이라는 원형. **조건:** 수집 장치(개수·가득 참·희귀한 돌·돌탑 높이) 금지, 짐("돌덩이")·던지기(물수제비) 연출 금지, 무거운 마음도 담도록 돌 크기 범위를 넓게, 은유는 숲·빛·조약돌·친구 넷으로 한정(마스코트·돌탑 제외), 삭제 같은 파괴 동작은 은유 없이 문구 그대로, 밤 장면은 PRD §6.3·D-034 예외 승인 필요. 감정 색은 돌에 박힌 가는 줄무늬로만 쓰고 나머지 돌은 청회색(`#27595B`, 공포700과 ΔE 10.5로 이웃 계열이라 구분해서 사용). 2주 시범 제안: 세부 감정 고르기의 체크·알약 점 자리와 달력 표시에만 조약돌을 써 보고 효과가 없으면 되돌린다 |
| 14 | 조약돌 예시 | 사용자가 "예시로 한번 만들어봐"라고 요청 | **만듦(결정 대기).** 실제 시안에서 `http://localhost:4174/?theme=pebble`로 눌러 볼 수 있다. 주소에 `?theme`이 없으면 기본 시안 그대로다(회귀 확인: 색 `#FFFFFF`/`#1F1F1D`·9종 포즈 줄·배너·문구 동일, 테마 파일 미로드). 담은 것 — ① 오늘 위쪽 밤의 시냇가 장면(내 빛이 가까운 돌을 비추고 돌의 가는 줄무늬만 감정 색, 친구 9마리는 같은 크기·taxonomy 순서·세 무리)과 새 요소 셋(이번 주 길·오늘의 한 줄 질문·다시 만난 조약돌 — 전부 "예시" 표기) ② 감정 별자리 지도의 점이 반딧불이 됨 ③ 세부 감정: 알약 점·체크 자리가 조약돌 모양이고, 고르는 순간 조약돌이 트레이("주머니")로 날아감(움직임 줄이기면 날지 않음) ④ 완료: 오늘 고른 계열의 줄무늬가 박힌 돌이 길 위에 놓임 ⑤ 달력: 완료=깊은 면+작은 빛(돌처럼 살짝 찌그러진 칸), 임시저장=옅은 면+점선+연필, 없음=빈 칸, 오늘=고리 ⑥ 새 중립색(종이·깊은 청록 잉크 — 글자 15.3:1, 보조 글자 4.7:1 이상이라 기본의 4.42 미달 조합도 해소). **측정(360×740):** 15개 화면 가로 넘침 0, 저장·전송 0(localStorage·sessionStorage·cookie 0, 다른 출처 요청 0), 44px 미만은 기존 초성 줄 13개뿐(제가 넣은 배너 링크는 44px 버튼으로 옮김), 데스크톱 1280에서 장면 비율 유지, `quick`·`full` PASS, `web/` 색 리터럴 0건. 값의 정본은 `design/brand-theme-pebble.json`이고 시안은 읽기만 한다(서버 허용 목록에 이 파일 하나 추가). **한계:** 실기기·실제 OS 움직임 줄이기·스크린리더는 미확인, 글꼴은 D-034대로 시스템 글꼴(concept 페이지의 명조 제목 없음), 깊은 청록 여섯째 자리 미정, 그림자·그라데이션 없음(§11)을 이 예시에서 벗어남 |
| 15 | 브랜드 캐릭터 | 사용자가 참고 이미지 3장(Suntera 기분 앱·교실 커뮤니티 앱·moimoi)과 글 두 편(무드 정의 "Playful Minimalism + Mascot-driven Graphic UI + Soft Neo-Brutalism" / 브랜드 방향 "유년기의 시각 언어, 판단 없는 기록")을 주며 **얼굴 없는 트롤**(남이 정한 방식이 아니라 자기 방식으로 사는 자연의 존재)을 캐릭터로, **색으로 캐릭터를 나타내고**, 조약돌과 이야기를 잇고, **배경은 흰색**으로 하면 어떤지 냉철히 평가해 달라고 함 | **평가함(결정 대기, 시안 안 만듦).** ① 두 글이 서로 반대 — 글 1: 캐릭터가 화면 주인공·전면 색 면·선택하면 배경색 변화 / 글 2: 캐릭터는 그림 표식·화면을 알록달록하게 하지 않기·쓰는 화면은 조용하게 → 글 2를 뼈대로, 글 1은 입구 한 화면만. ② 이미지 3장은 모두 얼굴이 있고(마스코트의 엔진) 그림 방식이 서로 다르며(평면 고채도 / 평면 파스텔+검정 / 유리 구슬+붓) "Soft Neo-Brutalism"에 해당하는 요소(굵은 검은 외곽선·딱딱한 오프셋 그림자)는 없다. ③ 큰 면에 감정 색을 쓰면 안 되는 측정(`design/tokens.json`, 글자색 `#092326` 가정): 500단 명도 L* 기쁨 79↔미움 38(차 41)·채도 C* 36~77이라 D-033 "색은 식별 라벨이며 강도·순위를 뜻하지 않는다"에 걸림. 300단은 명도 차 22, 짙은 청록 글자 대비는 500단에서 미움 2.3:1·희망 3.8:1로 미달, 300단은 전 계열 6.5~12.2:1. 다색 장식 블롭은 감정 색을 쓰면 "오늘 여러 감정을 골랐다"로 읽혀 입구 형태는 서비스 색 한 계열의 명도 단계로. ④ 트롤(시트를 보기 전 판단 — 16번에서 갱신): '트롤'은 한국어에서 인터넷·게임 트롤로 읽혀 이름으로 위험, 얼굴을 빼면 마스코트가 아니라 오브젝트가 됨(선택이며 조약돌과는 더 맞음), 색 단독 식별은 WCAG 1.4.1·색각 이상·수집 세트(Care Bears류)에 걸림. 트롤이 9종을 대체하면 D-044·D-045(9종·크레용 스타일)와 캐릭터 파일 95개 번복이라 DECISIONS 새 행이 먼저이고 캐릭터 재생성은 이번 범위 밖. ⑤ 잇는 법: 큰 돌(트롤 형태, 얼굴 없음, 서비스 색 단색)+작은 조약돌 한 쌍(에이전트 제안); 햇빛에 돌이 되는 민담 그대로는 "빛=제압"이 되어 받아들이기와 반대. ⑥ 흰 바탕은 PRD §6.3과 일치해 밤 장면 예외(§6.3·D-034·§11)가 사라지지만 빛 번짐 은유의 무대가 없어져 밤 시냇가 예시의 장면 셋(오늘 위쪽·감정 지도·완료)은 접거나 줄여야 함. 위기 절차(SAFETY_POLICY 4항)에서 캐릭터 연출을 끌 수 있어야 하므로 캐릭터가 브랜드 언어의 중심이면 그 화면만 톤이 끊김 |
| 16 | 예시 캐릭터 | 사용자가 "예시 캐릭터"로 평면 벡터 9종 시트를 주며 참고하라고 함(대화 첨부, 저장소에는 보존하지 않음). 눈 두 점·입 없음·부리·검은 신발, 자세로 감정을 말하고, 몸 색은 감정 색에 가깝고, 배열은 D-044의 taxonomy 순서 그대로 | **평가함(결정 대기, 시안 안 만듦).** **풀린 우려:** 색 단독 식별(실루엣·자세가 함께 식별), 얼굴 없음의 오브젝트화·섬뜩함(눈 두 점이 "누군가"를 만듦), '트롤' 이름(트롤로 읽히지 않아 이름이 필요 없음). **측정(1254px 시트, 면적이 큰 색 군집 vs `design/tokens.json`):** 몸 색↔자기 계열 토큰 ΔE 미움 1.8·사랑 3.1·분노 4.5·기쁨 5.0·공포 7.0(700)·혐오 10.0·즐거움 10.4·희망 14.2, **슬픔은 자기 계열이 아니라 공포와 가깝다(공포 6.6 < 자기 14.7, 기준 7 미달 — 시트의 슬픔은 청록 쪽, 토큰은 파랑).** 캐릭터끼리 몸 색 ΔE 최소 10.9(즐거움–혐오). 색각 이상 시뮬레이션(Machado 2009): 즐거움–기쁨 2.2~3.0(녹색맹·적색맹), 희망–슬픔 6.9(녹색맹), 기쁨–혐오 7.0(적색맹). 비교로 토큰 500단은 녹색맹에서 즐거움–혐오 0.6이라 색 붕괴는 시트만의 문제가 아니다. 44px 실루엣은 즐거움·기쁨·분노·혐오가 비슷하고(팔 벌린 형태) 슬픔·사랑·공포·미움·희망은 구별되며, 눈은 44px 이하에서 사라짐. **남은 문제:** D-044(각각 다른 동물·종 단위 행동과학 근거)·D-045(크레용 스타일) 번복과 캐릭터 파일 95개 무효, 자세 근거가 희망·사랑·미움·즐거움에서 약함(9종별 근거 검증은 안 함), 분노 뿔·슬픔 녹아내림이 무거움, 사탕 채도·부리·큰 신발이 성인용 노트보다 아이 캐릭터 상품 쪽(글 2의 경고 — 무표정 자체는 오히려 성인 톤), 시트의 3×3 정렬을 화면에 그대로 쓰면 사용자가 지적한 "일렬로 딱딱함"이 되풀이됨, 생성물의 재현성·권리 확인 필요. **제안:** 채택 전에 눈으로 비교 → 18번: 사용자가 잠정 채택으로 진행하라고 함 |
| 18 | 캐릭터 잠정 채택·벡터 | 사용자: "잠정 채택으로 기록하고 화면 만들어. 지금 ui를 수정하지 말고 임시로 만들어서 보여줘" → 에이전트가 화면 3장(오늘·고르기·완료) 비교를 만들던 중 사용자 정정: **"아니 캐릭터만 벡터로 다시 그려서 보여달라고"** → "너가 만든 캐릭터 보여줘봐" | **잠정 채택 기록(D-048 provisional)과 캐릭터 벡터 초안을 보였다.** ① 화면 비교 페이지는 접었다(백업은 세션 임시 폴더). `design/character-compare.html`(4173)은 이제 벡터 보기 화면이다 — 벡터 / 시트 원본 / 차이(겹쳐 보기), 눈·신발 색을 짙은 청록으로 바꿔 보는 미리보기, 높이 96·64·44·32px 줄, SVG 내려받기. ② 시트를 **그대로 따라** 벡터화했다: 평면 색 팔레트(4~8색)를 재고 색 영역마다 위에서 아래로 겹치는 층으로 나눠 하위 픽셀 윤곽을 따 3차 베지어로 맞춤(캐릭터당 층 4~8개·마디 99~251개·SVG 4~10KB). 눈은 시트에서 잰 크기의 타원, 바닥 그림자·감정 글자 없음, 층마다 `class`(`c1…` 색 면, `ink` 눈·신발). ③ 원본과 1:1 대조(Edge 렌더): 색 오차 평균 6.6~9.4/255이고 큰 오차는 거의 윤곽 1픽셀 안팎. ④ **알려진 차이:** 공포는 발 위 두 앞발(같은 청록의 미세한 톤 차이)과 팔 사이 이음선이 하나로 합쳐졌고, 혐오의 가운데 볏 끝이 살짝 뒤틀렸으며, 이음선·미세한 톤 차이는 면 색 방식이라 따르지 못한다. 분노·사랑 신발 밑의 톱니는 시트의 발판 무늬다. ⑤ **넣지 않은 것**(사용자가 "그대로"라 함): 슬픔 파랑, 즐거움·기쁨 구분, 분노 뿔·슬픔 자세, 채도, 눈 크기 통일, 청록 잉크(보기 화면 미리보기만). 파일 `design/temp-flat/vec-*.svg`(9)·`vec-sheet.svg`·`ref-*.png`(9, 같은 좌표의 시트 원본)·`source-sheet.webp`(사용자 시트 사본, 확정되면 `references/source`로). 이번 지시 이후 `web/`은 에이전트가 바꾸지 않았다. **→ 이후(2026-09-21 사용자 결정, 23번):** 앱 자산은 벡터가 아니라 고해상도 래스터로 간다(D-051의 1254px 투명 PNG). 이 벡터 초안은 비교 이력이다 |
| 17 | 하단 탐색 | 사용자가 10개 시안 중 2행 3번(짙은 숲색 pill + 라임 유기형 선택 면)을 고르고 "코드로 만들어봐", 이어서 "동글~ 하면서 네비게이션이 움직이게" 요청 | **반영.** 전: 흰 바탕 56px 바, 아이콘+보이는 라벨, 현재=진한 글자·filled 아이콘. 후: 72px+안전영역의 숲색 pill, 라임 blob 하나가 탭 사이를 320ms에 이동하고 도착할 때만 눌렸다가 둥글게 돌아온다. 탭은 아이콘만 보이되 보조기술 이름(오늘·달력·통계·설정)을 유지한다. 선택 상태는 blob+filled/outline으로도 구분하고, reduced-motion이면 즉시 전환한다. 서비스 `ink`/`lime`은 감정의 의미나 기록 상태에 쓰지 않는다(D-049). |
| 19 | 브랜드·서비스 스토리·디자인 시스템 전면 개정 | 사용자: "지금 정보와 폴더를 대조해 다른 점 브리핑", "서비스 스토리 정리", 이어 브랜드 방향 문서·근거 표·무드 글 두 편·행복/자유 정의를 주며 "서비스 스토리와 디자인 시스템을 전부 고칠 거야. 질문을 최대한 해서 방향을 픽스하고, 왜 이런 선택을 했는지 기록도 파일로 남겨. 기존 파일은 수정해" | **처리(결정 D-050·D-052~D-058).** 질문 7라운드로 확정: 감정별 테마(D-053), 서비스 색 ink·mint·크림빛(D-054), 알아차림의 빛(D-055), 조약돌 채택·돌 하나=그날 고른 계열 하나(D-050), 오늘 화면 구성과 한 줄 질문(D-056), 타이포·웹폰트(D-057), 이름 미정(D-058), 서비스 스토리 정본(D-052). 새 정본 `docs/BRAND_STORY.md`·`docs/DESIGN_RATIONALE.md`(왜 골랐나·대안·측정), `DESIGN_SYSTEM.md` 개정(§3.3·§3.4·§6.12·§6.13·§8), `tokens.json`(mint·light·hero·웹폰트·theme-field), `check-contrast`(292건), 전시장, PRD §6.3 한 줄, UX_SPEC·README·TRACEABILITY·RISK(RK-020~024). 같은 시간에 다른 세션이 친구 9종 최종 채택(D-051)을 기록해 반영했다(D-044~D-046은 superseded, D-048은 accepted). 오늘의 한 줄 질문 **목록 작성은 추후 계획**(사용자 지시) |
| 20 | 서비스 강조색 | 사용자: "다른 색 추천해봐", 이어 "아예 하늘색은 어때?(#00F7FF·#B0FFFA), 네 생각 냉철하게" | **민트 `#62FDB4` 확정(D-054).** 감정 토큰·그림 색과의 색차로 색상환을 탐색해 민트~스프링 그린 띠가 ΔE 14~17로 가장 비어 있음을 확인했다. 하늘·시안은 슬픔·공포 사이라 ΔE 7 미달(시안 5.2·연한 아쿠아 9.3·하늘색 6.5)이고 ink와 같은 색상각이라 반대했으며 사용자가 민트를 유지했다. 에이전트 정정: 색각 이상에서는 민트도 무너지므로(적색맹 기쁨 100과 0.5) 형태 단서가 필수다 |
| 21 | 목표 스타일 재확인 | 사용자가 레퍼런스 3장(Suntera 기분 앱·교실 앱·moimoi)을 다시 보내며 "이런 스타일의 UI/UX를 하려는 것 알고 있지?" | **알고 있음. 그러나 당시 결정(계열 50 면)이 그보다 얌전했다고 인정했다.** 에이전트의 과정 오류(하위 에이전트에게 "더 조용한 쪽" 지시)를 "조용함은 직접 작성 화면에만, 나머지는 대담하게"로 정정했다. 진하기 50·300·500을 같은 화면으로 비교해 사용자가 **300**("b.")을 골랐고(D-053 수정) 계열 선택은 **별자리 지도 유지 + 위쪽에 큰 친구 영역**을 골랐다(D-041 유지). 표정 있는 마스코트·손글씨는 의도적 차이로 유지 |
| 22 | 세부 감정·강도 흐름 재설계와 전체 UI 전환 | 사용자(30분 이상 자리를 비우며): "내가 다시 메시지 입력하기 전까지 전체 UI를 지금 결정된 무드로 전부 바꿔줘. QA도 네가 화면 보면서 진행해. 세부 감정 선택 UI는 다시 해야 해 — 하나의 감정을 선택하면 하나의 화면에서 그 감정의 세부 감정을 고르고 다음으로 넘어가 또 다른 선택한 감정의 세부 감정을 고르게, 배경은 그 색, 고르면 조약돌이 자연스럽게 모이는 애니메이션. 강도는 대표 감정이 메인, 그 밑에 '세부 감정도 강도를 선택하고 싶어요'. 질문 기능은 쓰지 말고 최고의 판단만 해서 진행" | **처리(D-059 provisional).** 세부 감정=계열별 화면(300 배경, pill 구름, 24개 초과 계열만 검색, 최소 1개, 이 계열은 빼기), 조약돌 모임 일시 피드백, 강도=대표 감정 메인 + 선택형 세부 강도(**저장 계약 불변, fan-out**). 판단 근거는 DESIGN_RATIONALE §3.9. 질문 도구는 쓰지 않았다. 전체 UI 전환은 하위 에이전트가, QA는 메인이 헤드리스 Chromium(CDP)으로 캡처·측정해 진행한다 — 결과는 아래 체크포인트 |
| 23 | 그림 형식·강조색·조약돌·무드 뼈대 | 사용자: "고해상도 / 어떻게 할지 추천해봐 / 중심 은유로, 글로시 질감 일부 허용 / 화면 무드의 뼈대는 뭐노" — 에이전트가 앞서 낸 '정하실 것' 네 가지에 대한 답 | **네 가지 모두 다른 작업에서 이미 결정·구현돼 있었다(에이전트의 앞선 총정리가 이를 '대기'로 적은 것은 낡은 정보였다).** ① 고해상도 = D-051(친구 9종 1254px 투명 PNG + 160·480 파생본, 조약돌 9종 1254px + 128·320) — D-048의 벡터 계획은 "뒤집힘"으로 고쳤다 ② 강조색 = D-054(라임→민트 `#62FDB4`). 최종 그림 색으로 다시 쟀다: 라임은 누리 몸 2.8·즐거움 조약돌 3.5~3.7, 민트는 가장 가까운 것이 즐거움 300 15.4·즐거움 조약돌 15.9·숨이 옅은 청록 17.4 ③ 조약돌 중심 은유 + 글로시 예외(조약돌 이미지에 한해) = D-050 ④ 무드 뼈대 = BRAND_STORY §6·D-053·D-056·D-057. 시안(4174)을 360×740으로 캡처해 대조: 측정한 화면(오늘·작성·고르기·세부·강도·검토·완료) 가로 넘침 0·44px 미만 0, `quick` PASS. **찾은 것(제안, 결정 아님):** 세부 감정 화면(D-059 provisional)은 360×740·375×667에서 첫 화면에 고를 선택지가 0개(첫 pill y=588, 아래 막대 시작 595·522), 390×844에서 6개다 — 친구 영역이 스크롤하면 접히지만(`.compact`) 차지하는 자리는 216px 그대로라 첫 화면은 줄지 않고, 아래 막대 2겹(버튼 73 + 탐색 72)이 나머지를 덮는다. 파일을 바꾸지 않은 모형(친구를 처음부터 120px + 작성 중 하단 탐색 숨김)에서는 6개가 보인다. 세부 감정 화면의 친구 200px 자리는 480px 파생본이 2.4배라 3배 화면 밀도에서 20% 덜 선명하다(계산, 실기기 미확인). `web/`은 바꾸지 않았다 |
| 24 | 무드 대조와 전체 변경 제안 | 사용자: "우리 근데 디자인 무드를 바꿨잖아. 어떤 걸로 바꿨는지 확인하고 현재 ui에서 전체적으로 어떻게 바껴야 하는지 생각해봐" | **제안함(결정 아님, `web/` 무변경).** 무드 = Playful Graphic UI(BRAND_STORY §6·§7, D-050~D-057). 현재 UI를 360×740 첫 화면 15개 상태로 재서 **새 언어 5**(오늘×2·완료·계열 고르기·세부 감정)·**부분 2**(강도·달력)·**옛 언어 8**(작성 5·검토·통계·설정)로 나눴다. 새 화면은 색 면 21~91%·최대 44px·친구/돌 있음, 옛 화면은 색 면 0~8%·28px 제목·글자 중앙값 14px·윤곽 상자 4~7·그림 0이다. 말투도 합니다체 약 27줄·해요체 약 29줄(탭 화면 12:4). 입력 윤곽 `#C9C7BF`는 배경과 1.62:1이다. **제안:** 화면별 패치 전에 옛 언어 부품 6가지(글자 단계·윤곽→채움·모양 가족·색의 무게 중심·움직임·말투)를 새 언어로 바꾸는 전체 패스를 먼저, 그다음 보드 4장(검토·달력·통계·설정)과 작성 흐름 골격. 층을 넷으로 고쳤다 — 무대(오늘·완료)·감정 면(고르기·강도)·보드(달력·통계·설정·검토, 조용할 필요 없음)·종이(직접 작성, 조용함). 지난번 "한 곳만 고치자"는 오늘·고르기·완료만 보고 한 말이라 좁았다. **새 결정이 먼저 필요한 것:** D-057에 display 글자 단계, 채운 입력의 경계 대비 방식, D-054 숲색 면 범위, D-056 이번 주 길 재사용, 작성 흐름 헤더·하단 막대 |
| 25 | v4 시안 — 오늘 숲·편지·통계 친밀도, 전체 IA, 추가 화면 45장 | 사용자: 온보딩·오늘 캐릭터의 흰 테두리 제거 / '이대로 남길까요'를 나에게 쓰는 편지로(봉투를 열면 고른 조약돌이 나오고 카드뉴스처럼 스와이프, 템플릿 ①감정·세부감정·크기 ②있었던 일과 이유 ③칭찬, 수정 버튼을 무드에 맞게) / 세부 감정 검색창 삭제·화면 전체를 감정색으로 / 오늘=어두운 숲, 길은 가운데, 나무는 뒷배경, 버튼 대신 가운데 돌(흔들림·호버 흰빛·클릭하면 흰빛이 전체로), 캐릭터 제외, 탐색은 청록 회피 / 완료는 그대로 두되 그날 고른 것만 / 통계는 캐릭터 친밀도 / '안전이 먼저예요' 삭제 / 마지막으로 "전체 IA를 그린 뒤 필요한 화면을 전부 시안으로, 질문 금지, 모든 권한과 판단을 위임" | **시안만 만들었다 — `web/`·`docs/DECISIONS.md`·DESIGN_SYSTEM·BRAND_STORY·토큰은 무변경, 산출물은 세션 scratchpad(`v4-sheet-1~9.png`, `ia.png`, `today-standalone.html`, `letter-standalone.html`).** 위임에 따라 판단한 것: 탐색은 어두운 장면 전용 크림 알약+ink 방울(민트 방울은 크림 위 1.17:1로 활성 표시가 약함), 돌 누른 뒤 첫 화면은 UX 정본 순서의 1/10 '오늘 있었던 일', 편지 장수=감정 수+2(감사는 채운 날만 +1), 고치기=테이프 붙은 태그, 친밀도=기록한 날 중 함께한 날의 비율을 위치로만(레벨·점수·보상 없음), 화면 말은 '강도' 대신 '크기', 접근 코드·온보딩②·기록 상세=편지 재사용·AI 화면(후행, 정적) 등 추가 화면 45장. **확정되면 새 결정 행이 먼저 필요한 것(뒤집는 결정):** D-050(어두운 밤 장면 철회·화면 바탕 흰색·무대는 풍경이 아님·레벨/누적 표시 금지와 친밀도의 경계), D-055(빛=크림빛·저장 뒤 → 시작 때 흰빛), D-056(오늘 화면 구성·친구 사용처), D-041·D-053(별자리 지도·독립 패널), D-036·DESIGN_SYSTEM 위기 안내 제목('지금 안전이 먼저예요' 삭제), D-049(탐색의 어두운 장면 변형), UX_SPEC §4 검토(수정 링크 → 편지 카드), BRAND_STORY §7·§8. 위기 연락처는 검수 전이라 번호를 쓰지 않았고 AI 분석의 '가능한 해석' 칸은 자리표시다. 완료 화면의 다이컷 흰 테두리는 '지금과 동일' 지시대로 남겼다 |
| 26 | 마음 고르기 | 사용자(2026-09-22): "오늘은 어떤 마음이었나요에서 친구 고르는 것을 친구들이 좀 자유분방하게 있고 첨부 이미지처럼 배경에 애들이 있되, 배경은 초록색 우리 프로젝트 컬러 계열로만. 그리고 조약돌은 없게 해서 시안 보여줘봐" | **시안 반영(D-066 provisional, 사용자 확인 대기).** 초록 언덕(`land.hill-*`) 위에 아홉 친구가 같은 크기로 자유 배치(조약돌 없음)·흰 길 하나·하단 버튼 줄. 이전 3×3 격자는 `#/write?pick=grid`로 남김. 오류 문구 대비가 원색 4.38:1이라 ink 25%를 섞어 6.2:1로 고쳤다(처음 손계산 6.49는 틀렸다) |
| 27 | 오늘 화면 개정 | 사용자: "길은 회색 / 조약돌은 주변이 아니라 배경에 자연스럽게 녹아들게(원근법), 호버하지 않으면 빛나지 않게 / 가운데 조약돌 클릭 시 전체가 빛나게 / '오늘은 어떤 마음이 머물렀나요?'를 타이포그래픽으로 강조 / 가운데 돌 호버 시 별 모양 삭제" → 이어서 "조약돌이 너무 많이 퍼져 있어. 감정당 하나씩만, 배경을 숲 속 안으로, 나무 옆에 작은 식물과 조약돌, 행간을 넓혀" | **반영(D-067·D-072).** 회색 길, 조약돌 아홉(22·30·42px 원근, 나무 뿌리 옆 식물 사이, 호버한 돌만 켜짐), 가운데 돌 클릭 시 아홉이 가까운 순으로 켜진 뒤 0.62초 뒤 흰빛, 별 모양 삭제, 큰 질문 타이포그래픽('마음' 가장 크게, 행간 0.7~1.15rem). 처음 만든 27개는 폐기. 결함 둘을 고쳤다 — 오늘 화면 문서가 화면보다 2px 컸던 것(탐색 음수 여백이 유리 윤곽·안전 영역을 빼고 있었음), 길의 아래 모서리가 유리 알약 밖으로 삐져나오던 것(길 아래 폭 330→280) |
| 28 | 세부 감정 | 사용자: "세부 감정을 선택하면 옆에 바구니 하나 둬서 바구니에 조약돌이 담기게" / "각 캐릭터의 메인색보단 살짝 연하게 배경 색을 해서 캐릭터가 잘 보이게" | **반영(D-068·D-071).** 바구니와 담기 연출(보이는 돌 최대 9개, 개수 글자 없음). 배경은 계열 100·50을 6:4로, pill은 300을 흰색에 45%. 친구 몸 색과 배경 색차 ΔE 최소 8.6→16.6, ink 글자 12.5:1 이상. 컴팩트(스크롤 시 영역 축소)가 한 번도 켜지지 않던 결함(sticky `top`과 판정 기준 어긋남)을 함께 고침 |
| 29 | 이야기 있는 화면들 | 사용자: "이대로 남길까요는 스토리텔링 형식으로 / 나머지 화면들에서도 너무 정적이지 않게 / 얼마나 컸나요와 이유도 스토리텔링, 이유는 하단에 고른 캐릭터 / 이유에 친구 이름뿐 아니라 느낀 대표 감정도 / 칭찬·감사·오늘 있었던 일도 이유와 비슷한 톤으로, 단 너무 똑같이는 말고 / 편지는 중앙에, 스크롤바는 내용이 길 때만·최대한 심플하게" | **반영(D-069).** 크기 '길 위의 친구', 이유(줄 노트+곁의 친구들+이름 아래 대표 감정), 오늘 있었던 일·칭찬·감사(아침 길의 들머리 / 쪽지와 해 / 꼬리표 카드와 등불), 편지(밝은 낮의 창가 책상, 정중앙, 넘칠 때만 4px 막대). 편지 배경은 밤의 책상→황토 나무 책상→옅은 민트 책상으로 세 번 바뀌었다(사용자의 '밝은 배경' 기준) |
| 30 | 달력·통계·설정 | 사용자: "달력과 통계 화면도 메인 화면에 맞게 / 분석의 '나' 동그라미 UI를 다른 무드와 맞추되 통계니까 살짝 정적인 건 유지" → **정정**: "아니아니 나머지 화면은 배경이 밝은 색이어야 해. 네비게이션도 원래 네비게이션 쓰게" + 흰 바탕 통계 시안을 첨부("내가 말한 무드 통일은 이런 거") | **반영(D-070).** 처음 만든 어두운 숲+유리 탐색 시안(작업자)을 되돌리고 밝은 바탕+원래 ink 알약 탐색으로 되돌렸다. 무드 통일은 색이 아니라 짜임 — 큰 '9월' 제목, 통계 친구 길 끝의 '나의 돌', 진입 연출 1회(통계는 반복 움직임 없음). 어두운 장면은 오늘 화면의 숲 한 곳뿐이다 |
| 31 | 탐색 | 사용자(2026-09-24, 캡처 첨부): "달력 네비게이션이 선택될 때 실루엣만 보이는 현상" → 활성 달력 아이콘의 머리띠 선이 ink 면에 묻혀 ink 덩어리만 남는다. 설정 활성의 원판·살 붙음과 탭 전환 중 새 아이콘이 ink 레일 위에서 사라지는 현상도 함께 | **반영**(T1, 2026-09-24): 활성 달력은 머리띠 선(`ic-cut`)과 날짜 칸(`ic-mark`)을 mint로 파서 ink 면 위에 달력 모양이 남는다 — 2x 캡처의 머리띠 줄 mint 102/170px, 날짜 칸 35/36px. 설정 원판 r 4.4→3.8(살과 틈 2x 3px). 탭을 옮길 때 새 아이콘의 ink 전환을 방울 이동의 55% 뒤로 미뤘다(`is-moving`). check-contrast·quick PASS |
| 32 | 통계 | "각 감정들이 그래프에서 우측으로 이동하는 속도가 너무 빨라" → 친구 걸음 400ms(줄 간격 50ms)를 느리게. 7/30일 전환 때 다시 재생되는 것도 D-070 위반이라 함께 | **반영**(Sub-D D1, 2026-09-24 메인 검수 통과 — Sub-A A3를 옮김): 걸음·채움 1200ms, 줄 간격 120ms, 걷는 동안 통통 4번, 자기 차례 전에 채움 조각이 보이던 문제(keyframe 0% 미정의) 고침, 7/30일 전환 때 재생 없음(연출 클래스는 진입 때만), 움직임 줄이기는 즉시 최종 자리. 통계는 `web/js/views/stats.js`·`stats-calc.js`·`stats.css`로 옮겼다(tabs.js의 옛 코드는 정리 대기) |
| 33 | 달력 | "날짜를 클릭하면 밑에 남은 화면에서 그 일기를 편지 형식으로" → 빈 자리(125~160px)에 편지(376px)가 안 들어가 **달력이 그 주 한 줄로 접히고 아래에 편지 인라인**(사용자 선택). ‹ ›로 달·주 이동 추가(사용자 선택) | **반영**(Sub-A A2, 2026-09-24 메인 검수 통과 — 줄 합치기 보완은 A5와 함께): 날짜를 누르면 그 주 한 줄로 접히고 아래에 그날 편지가 봉투 없이 인라인(scene:false)으로 나온다. ‹ ›는 한 달 보기에서 달, 접힌 주에서 주를 옮기고 이번 달·이번 주 뒤는 막힌다. 상태별: 완료=편지+크게 보기, 임시저장=[이어서 쓰기][이 글 지우기(2단계)], 지난 빈 날='이 날은 남긴 기록이 없어요.'만(쓰기 버튼 0, D-082), 오늘=[오늘 화면으로]. `?d=`로 고른 날을 연다. 완료 취소 뒤 `?d=`, 삭제 뒤 토스트, 없는·미래 날짜 주소는 '이 날의 편지가 없어요', 빈 달력 `?s=empty`, 불러오기·오류 세 곳. 메인 독립 측정: 375×812 스크롤 0, id 중복 0, 고른 날 aria-pressed 1개, region '9월 11일의 편지'. 남은 것: 360×740에서 편지 넘김 줄이 하단 탐색에 33px 가림 → 두 줄('한 달 보기'·'완료 칩+크게 보기')을 한 줄로 합쳐 푼다 |
| 34 | 오늘 | "숲의 밤하늘이 왜 초록색인지 모르겠어. 밤하늘 색으로" → 하늘과 나무 ΔE 1~3이라 하늘이 숲에 묻혀 초록으로 보였다. **짙은 남색**(사용자 선택). 밝은 남색은 미움900과 ΔE 3.6이라 제외 | **반영**(T2): sky-top/mid/low `#060B1A`/`#0B1228`/`#0C142A`. 흰 글자 18.56:1, 날짜 줄 11.43:1. 하늘 정지점 대 모든 감정 700·900 ΔE00 최소 7.70(미움900), 하늘-먼 나무 ΔE 21.6. check-contrast에 이 두 검사를 새로 넣어 384건 PASS. 지평선 안개·빛줄기는 민트→옅은 흰빛(달빛) |
| 35 | 완료 | "캐릭터들이 3*3으로 정직하게 배치… 자유롭게, '오늘은 어떤 마음이 머물렀나요?' 화면 참고, 크기 조금 키워서" → 마음 고르기처럼 손으로 정한 자리표. 크기는 **마음 고르기(약 70px)보다 조금 크게(4명 이상 약 88px)**, 1~3명은 지금 크기(사용자 선택) | **반영**(Sub-B B1 2회차, 2026-09-24 메인 검수 통과): 1~9명 손으로 정한 자리표(무작위 없음, taxonomy 순). 1~3명 210/164/136, 4~9명 88px, 레인 넷을 줄마다 엇갈려 써 마음 고르기처럼 바로 아래 겹쳐 선 쌍 0, 같은 높이 셋 없음, 4명은 사선(무게중심 169). 뒷줄은 먼 언덕에 서 머리가 하늘에 걸린다. 조약돌은 인원별 46→30px로 자기 친구 발 옆 땅 위. 기울기는 개별 속성 rotate(±7°), 입장은 친구마다 차례로(음수 지연 버그 고침). 메인 독립 측정(그림 픽셀 기준 `figure-measure.js`) n=1~9 × 375×812·360×740 18개 모두 통과 — 친구끼리 6px·친구↔조약돌 3px 이상, 해 고리·버튼 줄 침범 0, 조약돌 모두 땅 위·자기 친구 발치 한도 안 |
| 36 | 완료 | "오늘로 돌아가기 버튼을 빼고 달력 보기랑 통계 보기를 하단 버튼으로" → 하단 한 줄 [달력 보기(보조)] [통계 보기(ink 주)] — 주 버튼 하나 원칙(§6.1)에 맞춘 위계(사용자 선택) | **반영**(Sub-C C2, 메인 검수 통과): 제목 아래 링크와 '오늘로 돌아가기'를 없애고 하단 한 줄 [달력 보기(`btn secondary`)] [통계 보기(ink 주)] 같은 폭 — D-076 ③ 대체. `.done-bar`가 데스크톱에서 창 전체로 늘어나던 결함도 고쳤다(앱 열 480px 안). 메인 측정: 1280×800에서 버튼 줄 400~880 = 앱 열, 두 버튼 56px·폭 218. 쓰이지 않게 된 scene.css `.done-link*` 규칙은 메인이 지웠다 |
| 37 | 오늘 | "가운데 돌을 클릭하면 뒤에 감정 돌들이 좀 더 연하게 빛나게 / 흰색 화면으로 넘어가는 속도 조금만 더 빠르게" → 빛의 **세기**만 낮춤(사용자 확인), 흰빛 1500→1200ms | **반영**(T3): 조약돌 켜짐 빛무리 1→.55, 밝기·채도 1.15→1.0, 번짐 6→4px(색 유지). 흰빛 1500→1200ms(`motion.stone.flood`) — 돌 클릭부터 작성 화면까지 실측 1596ms(이전 1820). 돌 누른 뒤 화면을 떠나면 흰빛을 퍼뜨리지 않는다. 움직임 줄이기 250ms 불변 |
| 38 | 작성 | "크기 정하지 않았다면 팝업으로 어디 감정의 크기가 정해지지 않았다고" → 시트로 계열 목록 + '정하러 가기' | **반영**(Sub-C C1, 메인 검수 통과): 크기 단계에서 대표 크기를 정하지 않은 계열이 있으면 '다음'·'편지로 돌아가기' 모두 시트 '크기를 정하지 않은 마음이 있어요' + '설이 · 슬픔' 식 목록 + '정하러 가기'(첫 미정 슬라이더로 스크롤·초점)·'닫기'. 인라인 오류·aria-live 유지. 메인 측정: 두 이름·Esc 뒤 초점 '다음'·정하러 가기 뒤 `repr-sadness`·모두 정하면 시트 없이 이유 단계. quick PASS |
| 39 | 작성 | "이유 적는 칸에 나오는 캐릭터들도 좀 더 자유분방하게" → 줄 대신 인원별 자리표·기울기 | **반영**(Sub-B B2 2회차, 2026-09-24 메인 검수 통과, D-079 ②): 1~9명 손자리표(그림만 기울기, 라벨 수평), 발 높이를 흩어 격자로 읽히지 않음. 1차는 친구 자리를 위해 노트를 한 줄까지 줄여 되돌렸다(주 입력이 장식에 밀림) — 노트는 어디서나 163px(5줄) 이상, 친구가 넘치면 스크롤. 27개 조합(n=1~9 × 360×640·360×740·375×812) 끝까지 스크롤 기준 모두 통과. 움직임 줄이기 규칙이 주석 미종료로 죽어 있던 것도 고침 |
| 40 | 작성 | "이대로 남길까요 카드가 더 커도 좋을 것 같고, 세부 감정이 많아지면 1열로 쭉 길게… 너무 작아서 안 보여. 몇 개 이상이면 행을 늘리고 초과하면 카드 추가, 기준을 명확히" → '행을 늘리고'=여러 열(사용자 확인). 알약은 줄이지 않고 **4·8 기준**(1~4개 1열, 5~8개 2열, 9개 이상 8개씩 이어지는 카드), 카드는 화면 높이에 맞춰 키움 — D-074 대체 | **반영**(Sub-A A1, 메인 검수 통과 — 보완 1건은 A2와 함께): 카드 328×448(812 높이, 이전 300×376), 640 읽기는 376 하한으로 스크롤 0. 알약은 줄이지 않는다 — 1~4개 36px/15px 1열, 5~8개 32px/14px 2열(열마다 최대 4), 9개 이상 8개씩 이어지는 카드('슬픔 · 이어서 2/3'). 메인 독립 측정(4계열·알약 25개 작성 흐름, 375×812·360×740·360×640): 카드 6=점 6, 알약 최소 14px, 카드 밖·겹침·조약돌 겹침·구분선 넘김 0, 812·740 세로 스크롤 0, 640 작성 모드는 89px(RK-030 그대로, 악화 없음). 보완: 5개가 4+1로 나뉘어 외톨이 → 첫 열 ceil(n/2) |
| 41 | 오늘 | "맨 위 '오늘 마음을 아직 남기지 않았어요…'는 없어도 되지 않는지 생각" → PR-011의 유일한 MVP 인앱 알림이라 지우지 않고 **돌 안내에 흡수**(사용자 선택). 알림 켜짐+설정 시각(기본 오후 10시) 이후에만(사용자 선택) | **반영**(T4·T7): 위쪽 유리 배너와 `has-banner` 오프셋을 없애고 알림은 돌 위 한 줄 '오늘 마음을 아직 남기지 않았어요'(`role=status`) — 기록 없음+알림 켜짐+오후 10시 이후만(QA `?now=`). 20:00 없음·22:30 있음·이어 쓰기/기록 있음 없음을 캡처로 확인. 쓰이지 않게 된 `elevation.glass`·`forest.nav-icon` 토큰과 CSS 변수를 지웠다. 함께: 새벽 4시 전은 날짜 전날(01:30→9월 23일), 지난 날 쓰던 글이 있으면 '어제 쓰던 글이 있어요 ›'(44px), `?s=error|offline`은 돌 비활성+다시 시도. quick PASS |
| 42 | 설정 | "설정에 더 있어야 하는 것 생각" → PRD 필수인데 빠진 것(알림 시간·시간대, SAFE 개인정보 문구, 백업 삭제 지연, 위기 연락처 정적 칸, 내보내기 범위, 로그아웃)을 시안 자리로 + **하루 기준 시각(새 범위, 기본 새벽 4시)**(사용자 선택). 다크 모드 토글은 D-034로 제외 | 진행 중(Sub-A A4) **→ 반영·검수 통과(2026-09-25, A4 재작업 1회차)** |
| 43 | 전체 | "모든 화면에서의 예외처리도 생각해야 해" → 사용자 결정: 새 기록은 그날만·시작한 글은 마무리 허용, 쓰던 글 지우기(작성 ⋯·달력), 탭 동안 임시 보관, 도움 안내는 설정에서만(변경 없음), 전 화면 로딩·오류·연결 끊김 시안, 큰 글자 격자 전환. 나머지는 추천 기본값(계획 파일 '화면별 예외 처리') | 진행 중. T7 반영(오늘 화면 예외). **C3 반영**(Sub-C 2회차, 2026-09-24 메인 검수 통과): 날짜 칸 제거·지난 날 마무리 띠, 흐름 안 문구는 지난 날이면 '그날'(메인 재현: 마음 고르기 화면 글자에 '오늘' 0개), ×/이전 분리, 단계별 뒤로(4번 1단계·5번 오늘), **흐름을 떠날 때 흐름이 쌓은 방문 기록을 되감는다**(메인 재현: 달력에서 시작해 남긴 뒤 뒤로 1번 달력·2번 그 앞 화면, 오늘에서 시작하면 뒤로 1번 오늘, ×로 닫은 뒤 작성 단계로 돌아가지 않음), ⋯ 이 글 지우기 2단계+토스트, 자동 저장 표시·오프라인 띠, 새로고침 복구(sessionStorage), 만료 시트, 남기기 연타 잠금·실패 띠(한 줄, 오프라인 띠와 같은 48px), 수정 중 나가기 확인. 빈 날짜 탭 조각은 메인이 notes.js·notes.css에서 고침. A2(달력 예외)·B3 남음 **→ 반영·검수 통과(2026-09-25, B3 재작업 2회차)** |
| 44 | 문서 | "결정사항과 ui가 prd와 일치하지 않으면 prd가 오래된 것이니 prd도 수정" → 결정·화면과 어긋난 PRD를 동기화(결정 없는 제품 핵심 변경은 목록으로 보고) | **PRD·계약 문서 반영**(Sub-E, 2026-09-24 메인 확인): PR-008(새 기록은 그날만·시작한 글 기한 없이 완료), §7.2 날짜 입력 없음·흐름 이동·세션 임시 보관, §8 하루 기준 시각 `day_start_hour`(0~6, 기본 4), §9·§10 친구와의 친밀도·계열별 날 단위 평균·비교 규칙, DATA_MODEL(새 행은 서버 오늘만·§7 계산 정의), ARCHITECTURE(PUT 새 행 제한·조회 실패 행), EVAL_PLAN F-17~F-21, TRACEABILITY, RISK_REGISTER RK-027·RK-030 갱신·RK-031~033 신설, STATUS 갱신. full PASS. 결정 없는 불일치 없음. **디자인 명세 반영**(Sub-F, 2026-09-24 메인 확인): UX_SPEC §3·§4·§6·§7·§9~§12, DESIGN_SYSTEM 머리말·원칙 6·§3.4·§3.5·§6.7·§6.9·§6.11~§6.14·§8·§9·§12, DESIGN_RATIONALE §3.13 신설 — 옛 표기(하늘 3색·유리 배너·27개·42px·1500ms)는 이력 괄호 안에만 남음. 남은 것: D-085를 숲 속 빈터로(편지 화면 뒤), DESIGN_RATIONALE §3.10.2·§6의 sky-mid 측정 줄(메인) |
| 45 | 오늘 | 사용자(2026-09-24, 캡처에 네모): "첨부 이미지에서 네모 친 부분 수정해. 밤하늘이 세로로 선 있는거 없애고 어두운 네이비 단색으로만. 배경 디테일을 조금 손봐줘. 우리 디자인 무드를 적극적으로 살리되, 나무와 풀떼기랑 돌이 겹칠 수는 있는데 너무 현실성 없이 겹치는 것들은 수정해" → 네모 = 길 끝 위의 밝은 사각형(민트 안개 띠+지평선 흰 번짐+빛줄기)과 먼 나무 위에 덧칠된 길 끝. 조약돌이 모든 나무보다 위에 그려져 앞쪽 큰 소나무 잎 위에 떠 있었다 | **반영**(메인, D-084): 하늘은 `forest.sky` #0B1228 한 색(세 정지점 토큰을 하나로 합침, 빛줄기·안개·번짐 제거). 숲을 먼·가운데·가까운 세 겹 SVG로 나누고 조약돌을 자기 깊이의 겹 사이에 끼웠다(앞 나무가 뒤 돌을 가린다). 길은 먼 땅선(350)에서 멈추고 작은 나무 둘 뒤로 숲 속으로 사라진다. 가운데 땅 위의 길은 가운데 나무 앞으로 다시 칠한다. 가장자리 큰 소나무는 가지를 올려 발치의 땅·풀이 보이고 길 밖에 선다. 조약돌 아홉은 그 깊이의 땅이 보이는 자리에 손으로 두었다(먼 곳 둘·가운데 넷·가까운 곳 셋). check-contrast 348건 PASS(하늘 한 색 기준), 상태별(로딩·오류·이어 쓰기·어제 글·기록 있음) 안내 글이 숲 위에 뜨는지 elementFromPoint로 확인 |
| 46 | 편지 | 사용자(같은 날, 캡처에 네모 둘): "위쪽 직사각형 부분이 비는 건 괜찮은데 전체적으로 뒷 배경을 의미를 담아서 수정해봐. 아래 숫자와 카드의 간격이 너무 짧아. 좀만 늘려" → 창가 책상 장면(D-069)은 카드를 열면 창틀 띠·민트 면만 남아 뜻 없는 가로 띠로 읽혔다. 크기 숫자 글자 아래가 카드 가장자리에서 약 2px | **중단(2026-09-24 밤, 절반쯤 — 체크포인트 '중단' 줄)** 진행 중(Sub-A A5 방향 변경, D-085): 1차 '길의 끝' 시안(숫자 간격 20px는 통과)은 360px 땅 판·가로 이음매·깔때기 길로 되돌렸고, 사용자가 방향을 정했다 — '마음의 숲 속 빈터에서 나에게 보내는 편지를 열기 직전', 저자극·자기 성찰·안정감, 민트/그린, 봉투로 시선, 조약돌은 젤리가 아닌 돌 질감 **→ 재개 후 반영·검수 통과(2026-09-25, A5 재작업 1회차 — 이 행의 '중단'은 당시 기록)** |
| 47 | 통계 | 사용자(같은 날): "통계의 크기 추세는 어떤 로직을 사용할지, 심리학적 요소를 넣어서(거의 공통적으로 사용되는 의심의 여지 없는 것들로. 냉철하게 통계만 정리해도 됨) 깊게 생각하고 디테일화. 질문하지 말고 PRD·서비스 개요를 해치지 않는 선에서" → 지금 '크기 추세'·'평균 크기'는 모든 감정의 크기를 한데 평균 낸 값이라 서로 다른 마음을 섞고 기분 점수처럼 읽힌다(DATA_MODEL §7도 계열 단위 평균) | **반영**(Sub-D D1 2회차, 메인 검수 통과, D-086 — n=1 '크기 7'·n=2 '크기 6 · 5'·최소=최대 '모두 X', 빈 상태 캡션 보완 완료): 계열마다 작은 차트(점·선은 계열 강조색, 빈 날에서 선이 끊김), '평균 x · n일'(n≥3), 범위, 지난 같은 길이 기간 비교(Welch t 95% + 차이 1.0), 접힌 '읽는 법' 넷. 요약 칸은 기록한 날·이어서 쓴 날 둘('평균 크기' 0). 계산은 순수 함수(`stats-calc.js`, 서버로 옮길 수 있음). 메인 확인: 코드가 명세와 같음, 점 색 computed 확인, 가로 넘침 0, loading·error·few 상태. 작업자 확인: 독립 재계산 10/10행 일치, compare 자가 시험 9경우 통과 |
| 48 | 전체 | 사용자(2026-09-24): "자연스러운 하나의 영화처럼 느끼게 지금 편지 열고 편지지 사라지는 것처럼 연출을 몇개 넣어줘" → 이미 있는 흰빛 확산·봉투 열림·친구 입장과 같은 결로 끊기는 자리 셋: 작성 단계 사이(배경은 이어지고 내용만 방향 있게 바뀜), 남기기 뒤 봉인(카드가 봉투로 → 뚜껑 → 인장 → 흰빛 속으로 → 완료), 완료 → 달력(오늘 칸에 조약돌이 내려앉음). 움직임 줄이기에서는 끈다 | **중단(2026-09-24 밤, 구현 끝·검증 1/3 — 체크포인트 '중단' 줄)** Sub-C C4, `web/css/film.css` **→ 재개 후 반영·검수 통과(2026-09-25, C4 재작업 2회차, D-089)** |
| 49 | 달력 | 사용자(2026-09-25, 캡처): "달력이 그래도 한번에 다 보였으면… 아래 빈 공간을 활용하지 않는 이유를 알려주고 질문 기능을 통해서 수정 방향성" → 원인(카드 높이 상한 D-079·화면 높이를 보지 않는 한 주 접기 D-080) 설명 뒤 질문 3개(모두 추천안) | 전: 날짜를 누르면 한 주로 접히고 편지 카드 아래가 빔. 후: 한 달 유지 + 줄 노트 미리보기 종이 + 그 자리 펼침 | **반영(D-088)**, 1차 재작업(박스 안 빈 카드 → 줄 노트). 사용자 2차 지적('대충한 것 같다': 밑줄 링크·박스 속 노트) → 재작업 2회차 진행 중 |
| 50 | 달력·오늘 | 사용자(2026-09-25, 커밋 직전·뒤): '펼친 윗줄이 너무 빽빽하고 여백이 없다', '오늘 미리보기 종이가 전보다 낫지만 뭔가 이상하다', '오늘 화면 지난 기록이 너무 대충' → 2배 캡처 실측으로 원인 설명(HANDOFF-CAL-POLISH 가설 판정), 곳마다 세 안을 화면에 입혀 찍어 질문 | 선택: ① 윗줄 C(구조 유지·여백만 — 추천 A 편지 머리줄·무채 아이콘, B 회색 원은 기각) ② 종이 A(내용만큼의 쪽지 — B 크기 유지·수평, C 상태별 봉투는 기각) ③ 입구 A(달력 아이콘 알약 — B 원형 아이콘, C 밤하늘의 달은 기각). 안 캡처 `<SP5>/mock/s1.png`·`s2-25.png`·`s2-other.png`·`s3.png` | **반영·검수 통과(D-090)** — 오늘 입구 재작업 1회(큰 질문 12px 밀림), 달력 재작업 1회(마감 검토: 360×640 있었던 일 0줄·글자 앉힘 부족). 사용자 확인 대기 |

### QA-01 (2026-09-21) — 오늘의 정체성과 달력 상태

| 화면 | 사용자 말 → 확인한 뜻 | 전/후 | 처리 |
| --- | --- | --- | --- |
| 오늘 | 시작 화면의 요소가 적고, 2열로 정렬된 감정 동물들이 딱딱하다 → 직접 작성·AI 작성 버튼은 유지하되, 기록을 시작하기 전 "나만의 노트" 같은 밀도와 자유로운 숲속 탐험 분위기가 필요하다 | 전: 날짜·제목·설명·2개 시작 버튼·9종 캐릭터가 5+4 정렬. 후: 미정 | **검토 중.** 자체 점검 #12(핵심 버튼 위치·5+4 줄바꿈)과 겹친다. 사용자의 콘셉트 방향을 먼저 시안으로 비교한 뒤 가장 작은 구성 변경을 고른다. |
| 오늘 | 직접 작성·AI와 대화하며 작성 외에도 할 것이 있으면 좋겠다 → 새 저장 기능을 뜻하는지, 기존 달력·작성 흐름으로 이어지는 안내/짧은 시작 장치를 뜻하는지는 아직 확정하지 않았다 | 전: 2개 시작 버튼만. 후: 미정 | **보류.** PRD 밖의 새 기능을 임의로 넣지 않는다. 기능을 늘리지 않는 "한 문장으로 시작"·지난 기록으로 가기·작성 흐름 안내 같은 후보를 시안으로 제안한다. |
| 캐릭터 자산 | 희망 거북이 이미지에 햄스터(기쁨 쥐) 엉덩이 조각이 남아 있다 → 가장자리 혼입을 지워야 한다 | 전: `ui-poses/wish--expressive-static-*` 가장자리에 이웃 조각. 후: 미정 | **반영 예정.** 자체 점검 #1과 겹친다. 캐릭터 재생성이 아닌 국소 정리이지만, 현 범위의 "캐릭터 그림 재생성" 제외와 구분해 수정 방법·검수 기준을 먼저 제시한다. |
| 달력 | 연필·체크·기록 없음이 명확히 갈리지 않는다 → 감정 계열색과 별개인 서비스 상태색과 모양을 함께 써서 즉시 구별되어야 한다 | 전: 회색 테두리/점선/작은 연필·초록 체크. 후: 미정 | **반영 후보.** 자체 점검 #13과 겹친다. 현재 정본도 색만으로 구분하지 않도록 요구하므로, 기록 없음=중립 회색, 임시저장=경고 황갈색+연필, 완료=성공 초록+채운 점/체크의 조합을 시안으로 비교한다. |
| 제품 정체성 | 감정을 뜻하는 서비스 고유 색과 대표 캐릭터가 있으면 좋겠다 → 9개 감정 계열 동물과 혼동되지 않는, 제품 전체를 안내하는 중립적 표식이 필요하다 | 전: 9개 계열색·동물만 있음. 후: 미정 | **콘셉트 제안 중.** 기존 9개 계열색의 의미는 사용자 승인 없이 바꾸지 않는다(PR-010). 공통 브랜드색·대표 안내 캐릭터를 실제로 추가하려면 디자인 결정과 자산 쓰는 자리의 정본 변경이 필요하다. |

## 인수 조건

- [ ] 사용자가 말한 수정 요청마다 "전/후 화면"과 처리 결과(반영·보류·기각과 이유)가 이 파일에 남아 있다.
- [ ] 값이나 부품 규칙이 바뀌었으면 `tokens.json`·`DESIGN_SYSTEM.md`·`style-guide.html`이 같은 변경에서 맞춰졌고 대비 검사가 통과한다.
- [ ] 360px·데스크톱에서 가로 넘침 0, 44px 미만 요소 목록이 갱신돼 있다.
- [ ] quick·full PASS, PR CI 두 OS success, `package-lock.json` 무변경.
- [ ] 사용자가 고친 화면을 브라우저에서 직접 보고 승인했다.

## 체크포인트

- 완료(2026-09-21, 착수 절차 1~3): PR #30은 CI 두 OS success·병합 가능(`CLEAN`)이나 **병합 전**이라 사용자에게 물었고, **병합 없이 `web-ui/prototype`(`55f3f91`) 끝에서 `web-ui/qa`를 따라** 이어 가기로 했다. 병합은 사용자가 직접 하거나 지시할 때만 한다. 이 브랜치의 PR은 #30이 병합된 뒤 올리거나, 병합 전이면 기준(base)을 `web-ui/prototype`으로 해 순서를 지킨다. TASK-WEB-UI-01 절을 `tasks/archive/TASK-WEB-UI-01.md`로 옮겼다 — 원문 10,608B, SHA-256 앞 12자리 `75976470981a`. 검산: ① 옮긴 본문의 링크 대상 하나(`archive/TASK-ISSUE-26.md` → `TASK-ISSUE-26.md`)를 되돌리면 `git show 55f3f91:tasks/CURRENT_TASK.md`의 절과 같다 ② 잘라 낸 자리에 되끼우면 옮기기 전 파일과 같다. `harness/loop-state.json`의 `task_id`를 `TASK-WEB-UI-02`로 바꿨다. 미리보기 `web-prototype`(4174)·`design-preview`(4173)를 브라우저 pane에 열었다.
- 사용자 결정(2026-09-21, 착수 절차 4): QA는 **사용자가 먼저 시안을 둘러보고 지적을 한꺼번에 말한다.** 그동안 에이전트는 시안을 바꾸지 않고 전 화면 자체 점검만 해 둔다 — 사용자가 보는 화면이 도중에 바뀌면 지적의 기준이 어긋나기 때문이다. 지적을 받으면 자체 점검과 대조해 "겹친 것 / 사용자만 찾은 것 / 에이전트만 찾은 것"으로 나눠 보고한다.
- 이월: TASK-WEB-UI-01 절 끝의 "문서 밖 후속" — `tasks/TASK-MOBILE.md`·`tasks/TASK-DS-REF.md`의 "다음 번호 D-047" 표기는 낡았다(정본은 `docs/DECISIONS.md`의 D-039 행, 지금은 D-059). 이 파일들은 소유 파일 밖이라 마감 때 소유 파일에 더해 고칠지 정한다.
- 완료(2026-09-21, 자체 점검): 위 "에이전트 자체 점검" 표. 캡처·측정은 모두 읽기 전용이었고 시안·값·그림 파일은 바꾸지 않았다. `quick` PASS(착수 절차 정리 뒤). 브라우저 pane 함정 하나 추가 — 사용자가 pane 앞에 놓인 탭을 직접 눌러 보므로 에이전트가 그 탭을 스크립트로 몰면 사용자의 클릭과 부딪힌다(점검 탭이 사용자 조작으로 "통계"로 넘어갔다). 점검은 `tabs_create`의 background 탭에서 했고 그 탭의 screenshot도 동작했다.
- 완료(2026-09-21, 사용자 1차 지적): 위 QA 처리 기록. 그림 수정(거북이·기니피그 이웃 조각)은 반영하고 검증했다. 사용자가 QA보다 **브랜드 무드·컨셉을 먼저 정하자**고 해서 `design/brand-concepts.html`(전시장 4173, 소유 파일에 추가)에 세 안 — 도감 / 탐험 노트 / 숲 빈터 — 과 주인공 후보 3갈래를 만들었다. 서비스 색 후보의 색차는 프로젝트 기준(CIEDE2000, `check-contrast.mjs`와 같은 식)으로 쟀다: 진한 색(도토리 `#7A4E2D` 등)은 감정 강조색과 13 이상 떨어지고, 연한 종이색은 감정 50·100과 ΔE 2~8로 가깝다(지금의 `#FAFAF8`도 4.4) — 그래서 감정을 고른 표시는 면 색이 아니라 테두리·체크로 한다는 지금 규칙을 유지해야 한다. `web/` 시안은 아직 바꾸지 않았다.
- 결정 대기(사용자): ① 세계관의 세기 — 안 1/2/3 또는 혼합(에이전트 제안: 안 2를 바탕으로 오늘·완료 화면 위쪽에만 안 3의 풍경을 얇게, 작성 화면은 안 1처럼 조용하게) ② 주인공(사람/다람쥐/도토리)과 새 그림 제작 방법(Codex 이미지 생성 — 에이전트는 그림을 그릴 수 없다) ③ 손글씨 글꼴을 장식 문구에만 허용할지(D-034) ④ "그림자·장식 텍스처 없음"(DESIGN_SYSTEM §11)을 입구·출구 화면에 한해 완화할지. 정해지면 `docs/DECISIONS.md`에 D-050으로 먼저 기록한 뒤 `tokens.json`(서비스 색)·`DESIGN_SYSTEM.md`·`style-guide.html`·`web/`에 반영한다. D-036(로고·워드마크 없음, 새로 그리지 않음)과 이 작업의 범위 제외(캐릭터 그림 재생성)와 부딪히는 부분이 있고, 부품 규칙 보강이 커지면 보류 중인 TASK-DS-REF(D-039) 재개 여부를 사용자에게 먼저 묻는다.
- 결정 대기 추가(2026-09-21, 브랜드 스토리): ⑤ `#09232?` 여섯째 자리(어느 자리든 통과, 안 4는 `#092326`으로 그림) ⑥ 빛의 뜻 — "행복" 그대로(조건 3개 지킬 때) vs "알아차림, 행복은 그 결과"(에이전트 제안: 뼈대는 후자, 빛이 켜지는 자리를 칭찬·감사·알아차림 순간에 한정) ⑦ 자유의 색을 하늘색 대신 여백·길로 표현해도 되는지 ⑧ 어두운 밤 장면을 오늘·완료의 "입구 한 장면"에 한해 허용할지(PRD §6.3 "흰색/중립 배경 기본"·D-034 "밝은 모드 전용"과 부딪힘 — 승인 전에는 시안에만 둔다). 결정되면 D-050으로 기록한다.
- 결정 대기 추가(2026-09-21, 조약돌 은유): ⑨ 안 5의 조약돌 은유를 브랜드의 중심 은유로 채택할지(에이전트 제안: 채택하되 위 조건과 2주 시범) ⑩ 별도 마스코트(반딧불이 안내자·다람쥐·도토리)를 두지 않는 것으로 정리할지 — 빛은 "내 빛"이라는 사물이고 감정 그 자체는 조약돌이라 마스코트가 필요 없어진다. 채택하면 D-050에 은유 규칙(수집 금지·짐/던지기 금지·은유 넷·파괴 동작은 문구 그대로)을 함께 적는다.
- 결정 기록(2026-09-21, 캐릭터): 사용자가 **예시 시트의 잠정 채택**을 지시해 D-048(provisional)로 기록했다. 확정은 사용자가 벡터 초안(`design/character-compare.html`)을 눈으로 본 뒤에 한다. 남은 결정 — ⑪ 확정(D-044·D-045 번복, 파일 95개 무효) 여부 ⑫ 이름: '트롤'을 쓰지 않고 지금처럼 "친구"로 부를지 ⑬ 채택한다면 조건 — 슬픔 몸 색을 토큰 쪽(파랑)으로, 즐거움·기쁨 구분 보강, 64px 미만에서는 캐릭터를 아이콘으로 쓰지 않음(항상 라벨 동반), 분노 뿔·슬픔 녹아내림 조정, 눈·신발을 짙은 청록 잉크로, 시트 원본 보존과 벡터 재작성 ⑭ 흰 바탕으로 정하면 밤 장면 예외(⑧)를 철회하고 밤 시냇가 장면 셋을 접을지 줄일지. 조약돌 은유·화면 사용 규칙 등 나머지 대기 결정(①~⑩)은 D-050 이후로 기록한다(다른 작업이 조약돌 원본 9종을 `design/pebbles/`에 확정해 두었다).
- 예시 구현(2026-09-21, 조약돌): 표의 14번. 파일 — `design/brand-theme-pebble.json`(새), `web/css/theme-pebble.css`(새), `web/js/theme.js`·`sample.js`·`components/scene.js`(새), `web/js/main.js`·`tokens.js`(내보내기 한 줄)·`views/tabs.js`·`views/write.js`·`components/picker.js`(테마일 때만 갈라지는 몇 줄), `scripts/web-preview.mjs`(허용 목록 한 줄). 달력·이번 주 줄이 같이 쓰는 예시 상태를 `sample.js`로 모았다(달력 코드는 값이 같은 채 import만 바뀜). 사용자가 이 예시를 눌러 보는 중이며, **사용자가 앞 탭을 직접 눌러 보므로 에이전트는 그 탭을 조작하지 않는다**(테스트는 뒷 탭). 다음: 사용자 판단 — 조약돌 은유 채택(D-050 예정), 예시에서 고칠 곳.
- 검증 한계(2026-09-21): 브라우저 창이 가려져 캡처가 일부 비었다. 안 4의 위쪽 절반(밤 장면·CTA·이번 주 줄)은 화면으로, 나머지와 노트·색차 문구는 DOM 측정으로 확인했다(4개 안 표시, 가로 넘침 0, 그림 깨짐 0, 콘솔 오류 0, 친구 9마리 같은 크기·가장자리에서 14px 이상 안쪽). 반딧불 깜빡임은 3번 켜졌다 멈춘다(reduced-motion이면 정지). 페이지가 무거워 스티커는 120px 원본을 쓴다.
- 완료(2026-09-21, 조약돌 원본 확정): 사용자가 고른 9개 PNG를 `design/pebbles/`에 taxonomy 키(`enjoyment`, `wish`, `sadness`, `anger`, `joy`, `love`, `hate`, `fear`, `disgust`)로 복사하고, 같은 바이트의 원본 보존본을 `references/source/pebbles/`에 `--source` 이름으로 저장했다. 두 README에 감정·원래 파일명 끝 번호를 대응시켰다. 이 작업은 파일 보관·이름 정리만 하며, `?theme=pebble` 시안의 코드·D-050 예정 브랜드/사용 규칙 결정은 아직 바꾸지 않았다.
- 검증(2026-09-21, 조약돌 원본 확정): 9개 모두 1254×1254 `Format32bppArgb`이고 앱용·보존본 SHA-256이 각각 일치한다. `node scripts/verify.mjs --mode quick` 및 `--mode full` PASS.
- 완료(2026-09-21, 하단 탐색 D-049): 사용자가 고른 2행 3번 시안을 `web/`에 구현했다. service `ink #092328`·`lime #C8F75A`은 tokens·style guide·DESIGN_SYSTEM에 같이 추가했고, `main.js`가 라임 blob 하나의 위치를 바꾸며 320ms 이동·도착 bounce를 재시작한다. 360×740 Chromium에서 오늘·통계 양쪽을 캡처해 확인: 가로 넘침 0, 네 링크 각각 83.5×56px, `aria-current`와 접근 가능한 이름(오늘·달력·통계·설정) 유지, 선택 blob 52×52px. `check-contrast --verbose` service 조합 16.37:1·13.18:1 PASS, `verify --mode quick`·`full` PASS. 실제 OS reduced-motion·iPhone Safari·Android Chrome은 기존 사용자 검수 항목으로 남는다.
- 다음: 사용자의 선택을 받는다. 그 뒤 QA를 이어 간다(자체 점검 #2~#11·#14 다시 제시, 날짜 선택창, 달력 상태).
- 완료(2026-09-21, 캐릭터 최종 채택): 사용자가 평면 친구 9종의 채택과 이름을 확정했다. `design/characters/flat-friends/`에 영문 파일명(`nuri--enjoyment.png` 등)으로 정리하고 `references/source/characters/flat-friends/`에 `--source` 원본을 보존했다. 작명 이유와 감정 매핑은 `design/characters/README.md` §9, 결정은 D-051에 기록했다. 기존 크레용 동물 세트와 벡터 비교물은 이력으로 유지한다.
- 결정 완료(2026-09-21, 브랜드·디자인 개편): 위 "결정 대기" ①~⑭는 D-050·D-052~D-058로 정리됐다 — ① 세계관의 세기는 스토리 문서(`docs/BRAND_STORY.md`)에 흡수, ② 주인공은 친구(D-051), ③ 손글씨는 쓰지 않음(D-057은 Pretendard만), ④ 그림자·장식은 조약돌 이미지 한정 예외(D-050), ⑤ 여섯째 자리는 `#092328`(D-049), ⑥ 빛은 알아차림의 빛(D-055), ⑦ 자유의 색은 여백(D-054), ⑧ 밤 장면은 철회(D-050), ⑨ 조약돌 은유는 채택(D-050), ⑩ 별도 마스코트는 두지 않고 친구만, ⑪~⑬ 캐릭터는 D-051(슬픔·공포 색 근접은 알려진 한계, 64px 미만 라벨 동반은 DESIGN_SYSTEM §8.1), ⑭ 흰 바탕은 밤 장면 철회로 정리. 자체 점검 #2~#11·#14는 시안 재구성 뒤 다시 꺼낸다.
- 진행(2026-09-21, 시안 재구성): 하위 에이전트 한 명이 `web/`와 `scripts/web-preview.mjs`를 새 언어(흰 바탕 + 숲색 무대 + 감정 테마 300 + 조약돌·빛 + 친구)로 재구성한다. 그동안 메인은 docs·tokens·check-contrast·style-guide만 고쳤다(파일이 겹치지 않게 나눈 배치). 이 항목 아래에 결과와 검증을 이어 적는다.
- 동시 세션 기록(2026-09-21): 이 개편 중 다른 세션이 D-051(친구 최종 채택)을 결정 표 **밖**(설명 문단 뒤)에 붙여 두었다 — 표 안으로 옮기고 D-050(조약돌)·D-052~D-058을 그 사이에 이었다. 결정 번호 D-050·D-052~D-058은 이 개편이 예약해 썼다. 다음 신규 번호는 D-059.
- 이월(2026-09-21): 오늘의 한 줄 질문 목록 작성(추후 계획, `BRAND_STORY` §10), 앱 이름·로고(D-058), 설이·숨이 색 근접(알려진 한계), 글로시 조약돌과 평면 친구의 톤, 실기기(iPhone Safari·Android Chrome)·OS reduced-motion·색각 이상 사람 눈 검수.
- 자율 진행(2026-09-21): 사용자가 자리를 비우는 동안 질문 없이 최선의 판단으로 진행하라고 했다. 에이전트가 판단한 것 — 강도 fan-out(저장 계약 불변), pill 구름과 검색 기준(24개 초과), 돌 더미는 저장하지 않는 일시 피드백, 통계·설정의 새 무드 적용 수위. 사용자 복귀 후 눈 검수 대상이며 근거는 DESIGN_RATIONALE §3.9.
- 진행 중(2026-09-24, 오늘 화면 하단 탐색): 여러 라운드에 걸쳐 모서리 버튼+부채꼴·가운데 손잡이+길게 누르기 등 "숨겼다 꺼내는" 방식을 시안(Design 캔버스 아티팩트)으로 여럿 만들었다가, UX/HCI 이론으로 두 차례 재검토한 끝에 **숨기지 않고 유리 탐색의 존재감만 낮추는 쪽으로 반영**했다(D-075: `elevation.glass.fill` 16%→8%, `edge` 34%→20%). 대비는 오히려 올랐다(회색 길 배경 3.65→약 4.5:1). `check-contrast` 331건·`verify --mode quick` PASS. 논의 전체(아티팩트 링크·근거·열린 질문)는 별도 세션에서 이어 가려고 [tasks/NAV-TODAY-NOTES.md](NAV-TODAY-NOTES.md)에 정리해 두었다 — 이 파일은 결정이 아니라 작업 노트다.
- 완료(2026-09-24, 오늘 화면 하단 탐색 결론): 사용자가 바꾸려는 이유가 미적인 것이라고 밝혀 다시 비교했다 — 같은 화면 위에 스타일을 입힌 캡처로 여섯 안을 재고, 사용자 선택(통계 주 1~2회·미적 우선)으로 **D-076: 오늘 화면에는 하단 탐색 없음**, 날짜 줄 `지난 기록 ›`, 밝은 화면 네 탭 유지, 완료 화면 제목 아래 `달력 보기 ›`·`통계 보기 ›`. 반영: `web/`(today.js·write.js·base.css·scene.css·forest.js·tokens.js 주석), DECISIONS(D-076, D-075 superseded, D-062 ④ 표기), UX_SPEC §2·§3·§4-9, DESIGN_SYSTEM(원칙 6·§3.5·§6.11·§6.12·§6.13·접근성), DESIGN_RATIONALE §3.11, tokens 설명, style-guide 견본, check-contrast(유리 탐색 3건 → 날짜 줄 1건, 329건 PASS). 검증 증거와 남은 것은 [NAV-TODAY-NOTES.md](NAV-TODAY-NOTES.md) §7. 다음 신규 결정 번호는 D-077.
- 진행 중(2026-09-24, 피드백 13건 — QA #31~#44): 계획은 사용자 계획 폴더의 `twinkling-munching-ritchie.md`(사용자 승인). 작업마다 완료 기준을 두고 통과해야 다음으로 간다. 메인은 T1~T4·T7(탐색·오늘 화면)과 모든 QA·문서·PRD를 맡고, Sonnet 하위 셋이 파일을 나눠 병렬로 한다 — Sub-A 편지·달력·통계·설정(letter.js·letter-scene.css·write.css 편지 구역·tabs.js·tabs.css·tabs-mood.css), Sub-B 자유 배치(land.js·reason.js·reason.css·meadow.js·land.css), Sub-C 작성 흐름(write.js·state.js·flow.css). **AGENTS §2의 '동시에 쓰는 작업자는 서로 다른 worktree'에서 벗어난다** — 변경 대부분이 커밋 전이라 worktree에는 최신 코드가 없어 쓸 수 없고, 사용자가 '같은 폴더·파일 나눠 병렬'을 골랐다(2026-09-22과 같은 방식). 공용 API 뼈대는 메인이 먼저 만들었다: `dom.js` renderStatus, `state.js` todayISO(새벽 4시 기준)·setNow·sessionDraft·discardDraft, `sample.js` effectiveToday·지운 임시저장. 다음 신규 결정 번호는 D-077(이 절 위쪽의 D-059·D-061 표기는 당시 기록이다).
- 결정(2026-09-24, PRD 대조에서 나온 쟁점 넷 — 사용자 답): ① 벤치마킹 원자료가 '지난 날짜 기록'을 시장 기본 기대(P0)로 분류하지만 **'새 기록은 그날만'을 유지**하고 그 비용을 D-082에 적는다 ② **하루 기준 시각(새벽 4시)은 모든 '오늘' 판단에 같게** — 기록 날짜·이어서 쓴 날·알림·통계 기간·서버 날짜 검사, 설정을 바꿔도 이미 쓴 기록 날짜는 그대로, 범위 0~6시, 값은 사용자 설정에 저장·내보내기에 포함 ③ **쓰던 글이 있는 날은 이어 쓰기 칩·'이어서 써요'가 알림 역할**(따로 문구를 더하지 않음, PR-011의 '완료 뒤 중단'과 맞음) ④ PRD·데이터 문서의 **'카테고리 비중'은 '친구와의 친밀도'(D-064)로 대체 확정**. PRD 불일치 조사 결과(약 40곳·PR 문구 9개·다른 문서 영향)는 Wave 3에서 반영한다. 옆 세션의 벤치마킹 분석 문서(`docs/research/benchmark-2026-09-analysis.md` L251)는 '새 결정 번호(D-077)도 기록되지 않았다'고 적었다 — 그 문서는 그 세션 소유라 고치지 않는다(지금은 D-077이 오늘 화면 결정이고 하루 기준·지난 날 쓰기는 D-081·D-082).
- 체크포인트(2026-09-24 오후, 피드백 13건 + 추가 요청 셋 — QA #31~#47):
  - 반영·검수 통과: T1~T4·T7(메인), C1·C2·C3(작성 흐름), A1(편지 카드), A2(달력 한 주 접기 — 줄 합치기 보완은 A5와 함께), B1(완료 자유 배치, 2회차).
  - 메인 직접 반영: 오늘 숲 D-084(하늘 한 색 `forest.sky`, 세 겹 앞뒤, 길 끝, 가지 올린 소나무, 조약돌 자리), notes.js 빈 날짜 탭 제거, 통계를 `web/js/views/stats.js`로 떼어 냄(main.js가 이리로 연결, `web/css/stats.css` 연결).
  - 진행 중(백그라운드 Sonnet): Sub-A A2 보완+A5(편지 배경 '길의 끝'·숫자 간격, D-085) → 다음 A4(설정), Sub-B B2(이유 자유 배치) → 다음 B3(큰 글자 격자), Sub-D D1(통계 크기 추세 D-086·걸음 1200ms·불러오기 상태), Sub-E(PRD·DATA_MODEL·ARCHITECTURE·EVAL_PLAN·TRACEABILITY·RISK_REGISTER·STATUS 동기화).
  - 결정 기록: D-078~D-086(DECISIONS). D-002·D-069·D-070·D-074·D-076·D-077 상태 칸에 대체 관계를 적었다.
  - 남은 메인 몫: 하위 결과 검수, UX_SPEC·DESIGN_SYSTEM·DESIGN_RATIONALE·BRAND_STORY 동기화(화면 작업이 끝난 뒤), `.land-*`·flow.css·land.css·stats.css 통합과 tabs.js의 옛 renderStats 정리(Sub-A 작업 뒤), `verify --mode full`, 카테고리별 전/후 캡처 보고.
  - 마감 방식(사용자 지시 2026-09-24): 전체 작업이 끝나면 계획(`twinkling-munching-ritchie.md`)과 사용자 요청 전부가 반영됐는지 **검토를 1회만** 하고(2026-09-24 사용자가 '최대 4회'를 '1회'로 바꿈) 빠진 것을 고친 뒤 마무리한다. 커밋은 지시할 때만.
  - 도구 메모: Git Bash에서 `shoot.mjs`가 Edge에 못 붙으면 PowerShell에서 절대 경로로 실행한다(원인 미상, 2026-09-24 재현).
- **중단 체크포인트(2026-09-24 밤, 사용자 지시 '즉시 마무리하고 다음에 이어갈 수 있게')** — 하위 에이전트 셋(Sub-A·B·C)을 작업 도중 멈췄다. 멈춘 작업의 코드는 working tree에 **검수 전 상태로 남아 있다**(되돌리지 않음). 멈춘 직후 `verify --mode quick` PASS. 커밋 없음.
  - 이미 끝나 검수 통과: T1~T4·T7, C1~C3, A1, A2(1차), B1·B2(v2, 노트 163px 이상·27/27), D1(통계 D-086), Sub-E(PRD·계약 문서), Sub-F(UX_SPEC·DESIGN_SYSTEM·DESIGN_RATIONALE §3.13), 메인의 오늘 숲 D-084(하늘 한 색·가지 올린 소나무·세 겹).
  - **A5 편지 배경 '마음의 숲 속 빈터'(Sub-A, QA #46) — 절반쯤.** 사용자 방향 전환(길의 끝 → 숲 속 빈터)을 받아 `web/js/components/letter.js`·`web/css/letter-scene.css`에서 길·해·나비·연필을 빼고 빈터·조약돌 자리를 새로 짜는 중에 멈췄다. 마지막 동작은 '조약돌이 무엇에 가려지는지 실측'이었다(조약돌 겹침 문제를 찾은 상태). 같은 묶음의 A2 보완(`tabs.js` unfoldRow 좌우 배치·bigLinkSlot)과 숫자↔카드 간격 20px는 1차 시안 때 들어가 있다. 다음: 화면을 캡처해 지금 상태를 보고, 아래 기준으로 끝낸다 — 한 장면(가로 이음매 없음), 낮은 대비의 둥근 나무로 둘러싼 빈터, 봉투 뒤 따뜻한 흰빛·가장자리 비네트, 길·해·나비 없음, 조약돌은 호 모양 배치·무광 돌 질감(filter+노이즈 마스크+닿는 그림자, 젤리 광택 없음), 이전보다 낮은 휘도 표준편차, 레이아웃·봉투·글·버튼 불변, 숫자 간격 20px 유지. 끝나면 D-085 문구와 UX_SPEC §4 8단계·DESIGN_SYSTEM §6.14·DESIGN_RATIONALE §3.12.2의 '길의 끝' 서술을 '숲 속 빈터'로 고친다.
  - **B3 큰 글자 격자(Sub-B, QA #39·#43) — JS 연결 끝, CSS 일부.** `land.js`에 `watchFreeGrid`(루트 글자 20px 이상 또는 실측 겹침 → `fl-grid`, ResizeObserver)·`anyOverlap`을 만들고 완료(`land.js`)·마음 고르기(`meadow.js`)·이유(`reason.js`) 셋 다 호출한다. 격자 CSS는 `land.css`에 완료·마음 고르기만 있고 **이유 화면(`reason.css`)의 `fl-grid` 규칙이 아직 없다.** 캡처·측정은 하지 않았다. 다음: reason.css 격자 규칙 → 루트 20px·24px 캡처(겹침 0)·16px에서 자유 배치 그대로·Tab 순서 확인.
  - **C4 영화 같은 전환(Sub-C, QA #48) — 구현 끝, 검증 1/3.** `web/css/film.css`(단계 전환 View Transitions 슬라이드+페이드, 미지원 대체, 봉인 연출 카드→봉투·뚜껑 닫힘·인장 누름·흰빛, 완료 등장, 달력 오늘 칸 조약돌 내려앉음 `body.film-just-saved`, 움직임 줄이기 대체), `write.js` `playSealSequence`를 `complete()` 성공 경로에 연결, `main.js` 라우터가 작성 단계끼리만 `startViewTransition`. 단계 전환은 실제 교차 페이드를 확인했고, **봉인 연출과 달력 조약돌은 프레임을 찍었지만 아직 보지 않은 채 멈췄다.** 다음: 봉인·달력 프레임 검수, 움직임 줄이기·저장 실패(편지에 머묾)·연타 잠금과의 충돌 확인, 뒤로 가기 함정(C3) 회귀 확인.
  - 그다음 순서: A4 설정(알림 시각·시간대·하루 기준 시각 0~6시·개인정보 문구·위기 연락처 자리(번호 미기재)·내보내기 설명·로그아웃 확인 시트, 모두 aria-disabled 시안) → 메인 정리(tabs.js의 옛 renderStats·SAMPLE·FEW·TREND·trendChart·MIN_DAYS와 tabs.css·tabs-mood.css 옛 통계 규칙 삭제, scene.css `.land-*` → land.css 통합) → `verify --mode full` → 카테고리별 전/후 캡처 → **마감 검토 1회**(대조표 아래) → 사용자 보고. 커밋은 지시할 때만.
  - 작업 도구(세션 scratchpad `C:\Users\NOHTAE~1\AppData\Local\Temp\claude\C--Users-Noh-TaeKyung-Desktop-emotion-diary\e4b5a3da-503b-40ef-8f93-fb34a2609485\scratchpad`, 임시 폴더라 사라질 수 있음): `BRIEF.md`(하위 작업자 공통 지침 — 접두어 Sub-A `lt2-/cal-/set-`, B `fl-`, C `wf-`, D `sx-`, 포트 9411/9421/9431/9441), `nav/shoot.mjs`(헤드리스 Edge 캡처, PowerShell에서 절대 경로로), `main/qa-b1/figure-measure.js`(친구·조약돌 알파 픽셀 겹침 측정), `review/CHECKLIST.md`(마감 검토 대조표: 원래 요청 13건 A1~A13, 과정 요청 B1~B7, 질문으로 정한 것, 오후 요청 D1~D6, 문서·검증). 폴더가 없으면 캡처 도구는 다시 만들고, 대조표는 이 절과 QA #31~#48 행으로 대신한다.
- 재개(2026-09-25): 사용자 지시 '이어서 작업해'. 멈췄던 Sub-A(A5)·Sub-B(B3)·Sub-C(C4)를 각자의 대화 맥락 그대로 다시 깨워 남은 기준(위 '중단' 줄)대로 끝내게 했다. scratchpad 도구·대조표는 남아 있었다. 착수 전 `verify --mode quick` PASS.
- A5 검수(2026-09-25): 배경(빈터·봉투 뒤 흰빛·비네트·이음매 없음, 휘도 표준편차 51.5→24.8)은 통과. 조약돌 결함 셋으로 재작업 1회차 — ① grain이 알파 밖까지 칠해져 조약돌마다 회색 네모 ② 닿는 그림자가 10px 떠 있음 ③ 캡션을 감싸는 목걸이 배치·24~30px로 작아 사탕처럼 보임 → 캡션 아래 땅 띠의 얕은 호·40~44px·채도 한 단계 낮춤. '열었을 때 조약돌이 카드 밖에 보여야 함' 조건은 뺐다(읽는 동안 시선은 카드).
- C4 검수(2026-09-25): 단계 전환 교차 페이드·저장 실패면 연출 없음·연타 1회만·뒤로 가기 함정 회귀 없음(c3 15개)·움직임 줄이기 즉시는 통과. 재작업 1회차 — ① 봉인 연출 뒤 완료 화면만 친구 약 55px·언덕 없음(직접 열면 226px·언덕 정상 — 메인 재현) ② 2500ms 프레임에 흰빛 잔존 ③ 달력 조약돌이 오늘 칸 위 테두리에 모자처럼 걸침 → 오른쪽 아래 배지 자리·300ms 흐려짐.
- 작업 방식 점검(2026-09-25, 사용자 요청 — Anthropic 공식 문서 대조: code.claude.com/docs/en/best-practices·/agents, anthropic.com/engineering/effective-harnesses-for-long-running-agents): 계획 먼저·화면 캡처와 측정으로 증거 제시·반드시 지킬 규칙은 훅·auto 모드·하위 에이전트 위임은 권장과 맞다. 다른 점과 이번에 바꾸는 것 — ① 마감 검토 1회는 메인이 아니라 **새 맥락의 하위 에이전트**가 diff를 계획·대조표와 비교하게 한다(문서: 일한 쪽이 채점하지 않게, '요구사항·정확성에 영향 있는 빈틈만' 보고) ② 같은 워커에게 재작업을 **두 번 보냈는데도 실패하면** 이어 붙이지 않고 그동안 배운 것을 담은 새 지시로 새 워커를 띄운다(문서: 두 번 고쳐도 안 되면 새로 시작). ③ 커밋되지 않은 변경이 100파일을 넘는다 — 문서는 단계마다 설명 있는 커밋으로 되돌릴 지점을 남기라 하고, 체크포인트는 Bash로 한 수정을 잡지 못한다. 커밋은 사용자 지시 사항이라 제안만 했다. ④ 같은 폴더 병렬은 파일을 나눈 채로 두되, 문서는 같은 파일을 만지면 worktree로 격리하라고 한다 — 공용 미리보기 서버 재시작·main.js 공동 수정·C4 원인이 Sub-B 파일에 있는 경우처럼 경계가 겹치는 일은 메인이 순서를 정한다.
- A5 통과(2026-09-25, 재작업 1회차): 조약돌 grain 마스킹(모서리 ΔE 0.000)·밑면에 닿는 그림자·캡션 아래 좌우 대칭 호(안쪽 40~44px, 바깥 작게)·채도 한 단계 낮춤. 캡션·봉투·버튼 줄과 겹침 0, 가로 넘침 0, A1 28-combo·숫자 간격 20px 회귀 통과. 전/후 비교 `<SP>/sub-a/a5-pebble-before-after.png`. 남은 것: D-085·UX_SPEC·DESIGN_SYSTEM·DESIGN_RATIONALE §3.12.2 문구를 '숲 속 빈터'로(메인).
- A4 착수(2026-09-25): 새 작업 방식 ②에 따라 긴 맥락의 Sub-A를 잇지 않고 새 작업자 Sub-A2(Sonnet)에게 맡겼다 — 소유 파일 `tabs.js`의 renderSettings·tabs.css·tabs-mood.css, 포트 9451.
- B3 검수(2026-09-25): 마음 고르기·이유 격자(20·24px)·16px 자유 배치 유지·클래스 반복 0회는 메인 실측으로 통과. **완료 화면 격자가 깨짐** — 친구 약 30px로 축소, 첫 칸이 2열에서 시작, 언덕 사라져 빈 흰 면 → 재작업 1회차. C4의 '봉인 뒤 완료 화면 작게' 증상과 같아 원인이 겹칠 수 있다.
- 작업방식 문서화(2026-09-25, 사용자 지시): `docs/AGENT_WORKFLOW.md` §10 '오케스트레이터-워커 병렬 작업'을 새로 쓰고 §3C·§5에서 연결했다. D-087을 기록하고 D-042에 갱신 표시를 달았다. AGENTS.md §2에 한 줄(§10 참조) 추가, `references/README.md`에 공식 출처 셋 추가. `verify --mode quick` PASS.
- B3 재작업 1회차 결과(2026-09-25): 9명 격자는 통과(친구 88px, 1행 1열부터 시작, 언덕 배경, 봉인 경로 16px 정상 91px — C4 결함 1의 원인이 B3였음을 확인). 재작업 2회차(D-087상 마지막): 1·3명에서 언덕이 격자 높이에서 가로 직선으로 잘림, 1명이 왼쪽 위에 홀로 106px로 섬 → 장면 영역 전체에 배경·3명 이하는 가운데 정렬·크기 min(자유 크기, 폭 55%).
- A4 검수(2026-09-25): 조작 44px·키보드·로그아웃 시트(Esc·포커스 복귀)·번호 없음·넘침 0은 통과. 재작업 1회차 — 'SAFE:53'은 SAFETY_POLICY 52~53행 문구(원문 인용, 바로 아래 위기 연락처) / API key·owner key·DB 등 개발자 용어를 사용자 말로 / 꺼진 스위치가 켜진 모양 / 행 설명을 각 행 아래로 / 위기 연락처 빨간 테두리 → 중립.
- A4 통과(2026-09-25, 재작업 1회차): SAFETY_POLICY 52~53행 원문 + 바로 아래 위기 연락처(번호 없음, 중립 테두리)·개발자 용어 0개(정규식)·꺼진 스위치 모양이 aria와 일치·행 설명을 각 행 아래로·하루 기준 시각 '새벽 4시'. 44px·Esc·포커스 복귀·루트 20px·넘침 0. 캡처 `<SP>/main/a4-strip.png`.
- 메인 정리(2026-09-25): ① D-085 문서 동기화 — UX_SPEC §4 8단계·§5, DESIGN_SYSTEM §3 제외 목록·§6.14, DESIGN_RATIONALE §3.12.2를 '숲 속 빈터'와 측정값으로 고쳤다. ② `tabs.js`의 옛 renderStats·SAMPLE·FEW·MIN_DAYS·TREND·trendChart와 쓰지 않는 import(stoneImg·FRIENDS·category)를 지웠다. `tabs.css`의 옛 통계 규칙 36줄과 `tabs-mood.css`의 tb-* 통계 규칙 20줄을 지웠다(.stats-screen .segmented 한 줄만 남김). 통계·통계 빈 상태·설정·달력 캡처 정상, 가로 넘침 0. `quick` PASS.
- B3 통과(2026-09-25, 재작업 2회차): 완료 화면 격자 — 언덕 배경이 장면 전체(버튼 줄 위 흰색 0%), 3명 이하는 가운데 한 줄·세로 40%(1명 198~206px, 2명 144~150px, 3명 97~101px, 9명 88px 3열 불변). 360×740·375×812 × 20·24px × 1·2·3·9명 겹침 0·넘침 0, 16px 자유 배치 figure-measure 9/9, 마음 고르기·이유 격자 회귀 통과. 캡처 `<SP>/sub-b/v3/strip2-b.png`. 남은 것: C4 재작업 결과 대기 → CSS 통합(scene.css `.land-*` → land.css) → full → 새 맥락 리뷰어 마감 검토 1회.
- CSS 통합(2026-09-25, 메인): scene.css의 `.land*` 규칙 14줄·lfin/lfbob/sunpulse 키프레임과 tabs.css의 `.land-fit` 2줄을 `web/css/land.css` 머리로 옮겼다(값 그대로, 움직임 줄이기 규칙도 함께). 완료 1·9명·루트 20px·온보딩·오늘 캡처 전/후 픽셀 차 0. `quick` PASS.
- C4 재작업 1회차 결과(2026-09-25): 봉인 뒤 완료 화면 정상(친구 235.7px, 원인은 B3 진행 중 상태), 흰빛 DOM에서 빠짐, 달력 조약돌 오른쪽 아래 배지·300ms 흐려짐·움직임 줄이기 통과, 연출 약 1.02초, 콘솔 오류 0, c3 회귀 통과. 재작업 2회차(마지막): 조약돌이 사라지는 순간 오늘 칸 모양이 민트 채움→잉크 채움으로 뒤바뀜 — 연출 내내 최종 모양 유지.
- 새 요청(2026-09-25, QA #49 달력 한 달 + 편지 미리보기): 사용자 '달력이 한번에 다 보였으면, 아래 빈 공간을 왜 안 쓰나' → 원인 설명(카드 높이 상한 D-079, 화면 높이를 보지 않는 '항상 한 주 접기' D-080) 뒤 질문 3개로 확정(모두 추천안: 달은 그대로+미리보기 / 조약돌 줄+한 줄 요약 / 그 자리 펼침). **D-088** 기록(D-080 ① 대체 표시). 새 작업자 Sub-A3(Sonnet, 포트 9471)에게 맡김 — 소유 파일 `tabs.js`의 renderCalendar·tabs-mood.css·tabs.css 달력 부분. 끝나면 UX_SPEC 달력 절·DESIGN_SYSTEM 달력 부품을 메인이 갱신.
- C4 통과(2026-09-25, 재작업 2회차): 원인은 달력 조약돌이 오늘 칸의 `::after`(완료 잉크 채움, tabs.css)를 가로챈 것 — 배지를 write.js가 만드는 별도 고정 요소(`.wf-film-badge`)와 box-shadow만 쓰는 빛(`.wf-film-glow`)으로 옮겼다. 오늘 칸 bg·border가 0~2500ms 전 시점에서 연출 없는 기준과 같음(375·360·움직임 줄이기), 콘솔 오류 0, c3 회귀 통과. 캡처 `<SP>/sub-c/c4-shape-fixed-strip.png`. **주의:** 배지가 오늘 칸의 화면 좌표를 재서 붙으므로 D-088(Sub-A3) 달력 변경 뒤 이 연출을 한 번 더 확인한다.
- D-088 검수(2026-09-25): 동작(한 달 유지·겹침 0·375×812 스크롤 0·펼침/닫기/뒤로/주소·초점·움직임 줄이기·오늘 칸 불변)은 통과. 디자인 재작업 1회차 — 빈 공간이 카드 안으로 옮겨 갔을 뿐이라, 모든 상태를 같은 **줄 노트 종이**로(남는 높이는 줄로 채움, 완료는 조약돌+감정 이름 한 줄·있었던 일/이유/칭찬·감사 순, 빈 날은 '비워 둔 종이'), 펼침 전환에서 옛 글자가 유령처럼 겹치는 것 수정.
- D-088 통과(2026-09-25, 재작업 1회차): 모든 상태가 같은 줄 노트 종이(완료 = 날짜·예시 / 조약돌 36px + 감정 이름 한 줄 / 있었던 일·이유·칭찬·감사, 빈 날 = 비워 둔 종이), 카드 안 빈 구간 13~21px, 펼침 전환 유령 겹침 수정. 동작 회귀 없음. 캡처 `<SP>/sub-a3/out/states-strip-v2.png`·`flow-strip-v2.png`. C4 달력 조약돌 연출을 새 달력에서 다시 확인 — 오늘 칸 오른쪽 아래에 정상으로 붙음.
- 문서(2026-09-25, 메인): **D-089**(이어지는 전환) 기록. UX_SPEC 달력 절(D-088)·8단계 뒤 전환 문단(D-089), DESIGN_SYSTEM 달력 부품(D-088)·움직임 절(D-089), DESIGN_RATIONALE §3.14 추가. `quick` PASS. 다음: 새 맥락 리뷰어 마감 검토 1회(대조표 `<SP>/review/CHECKLIST.md` + D-088·D-089 추가 예정).
- 사용자 지적(2026-09-25, D-088 디자인): '대충한 것 같다' — 밑줄 글자 링크(✕ 닫기·크게 보기·펼쳐 읽기)가 싸 보이고, 흰 박스 안에 노트가 든 박스 속 박스가 부자연스럽다. → Sub-A3 재작업 2회차(마지막): 미리보기를 테이프 붙은 종이 한 장(nt-paper 말투, 0.4deg, 모든 줄 2rem 격자), 종이 전체 탭 + 잉크 알약 '펼쳐 읽기', 펼친 윗줄은 원형 × 버튼·가운데 날짜·'크게 보기' 알약, 달력의 밑줄 링크 전부 제거. 이번에도 기준을 못 넘으면 메인이 직접 고친다(D-087).
- **마감 검토 1회 완료(2026-09-25, 새 맥락 verifier — D-087)**: 원래 요청 13건·추가 요청 D1~D11 대부분 ✅, 요구사항 영향 빈틈 8건. 처리:
  ① 알림 줄이 자정~4시에 사라짐 → `today.js`가 하루 기준 시각에서 잰다(01:30 알림 있음·20:00 없음·22:30 있음·05:00 없음 실측).
  ② 지난 날 쓰던 글과 알림 동시 표시 → PRD·DATA_MODEL·D-081 ③대로 쓰던 글(지난 날 포함)이 있으면 알림 없음(`?s=yesterday&now=22:30` 알림 없음·어제 줄 있음), DESIGN_SYSTEM §6.12 문구 정정.
  ③ 내보내기 스키마에 하루 기준 시각 없음 → `schemas/export.schema.json`에 `dayStartHour`(0~6, 필수), DATA_MODEL §8 최상위 목록 추가.
  ④ 통계 명세 옛 서술 → UX_SPEC §8·DESIGN_SYSTEM §6.9·style-guide 통계 견본을 D-086(요약 두 칸·계열별 작은 차트·n≥3·Welch 비교·7/30일)로.
  ⑤ 편지 배경 옛 서술 → UX_SPEC·DESIGN_SYSTEM·style-guide(설명·SVG 견본을 빈터로)·RISK_REGISTER RK-030.
  ⑥⑦ 설정 알림 스위치가 꺼짐으로 보임(시안 기본값은 켜짐)·'내보내기(JSON)' 용어 → `tabs.js`를 Sub-A3가 쓰는 중이라 D-088 재작업 뒤 메인이 고친다.
  ⑧ QA 표 상태(#42·#43·#46·#48)·#49 행 추가·파일 지도·STATUS 갱신. land.css 낡은 주석 정정. `quick` PASS.
- 이월(선택): D-088 미리보기가 첫 줄을 보여 옆 사람이 보는 상황의 숨김 설정 — 실사용 관찰 항목(D-088 후속 칸).
- D-088 통과(2026-09-25, 재작업 2회차 + 메인 마무리): 미리보기는 테이프 붙은 종이 한 장(0.4deg, 줄 격자 오차 0.11px), 종이 전체 탭 + 잉크 알약 '펼쳐 읽기', 펼친 윗줄은 원형 ×·날짜·'크게 보기 ↗' 알약, 달력 밑줄 링크 0. 메인이 직접 고친 것(D-087 — 마지막 재작업 뒤): 두 알약에 브라우저 기본 버튼 테두리(회색 고리)가 남아 `border:0`, 윗줄 가운데 '9월 23일 (수)의 …' 말줄임 → 날짜만(편지 뜻은 region 이름). 마감 검토 ⑥⑦: 설정 작성 알림 스위치 `aria-checked="true"`(시안 기본값 켜짐과 일치), '내 기록 내보내기(JSON)' → '내 기록 내보내기', 설명에 '하루 기준 시각 같은 설정' 추가. 캡처 `<SP>/main/final/strip.png`. quick·full PASS.
- 사용자 지적(2026-09-25, 커밋 직전): 펼친 윗줄이 너무 빽빽하고 여백이 없다. 오늘 미리보기 종이가 전보다 낫지만 뭔가 이상하다. → **원인 분석은 새 세션에서** 한다. 요청 프롬프트와 메인이 본 검증 전 가설은 [HANDOFF-CAL-POLISH.md](HANDOFF-CAL-POLISH.md). 다음 세션은 그 파일의 프롬프트로 시작한다.
- 커밋(2026-09-25, 사용자 지시 '커밋해'): 두 묶음으로 나눈다 — ① 다른 세션이 끝내 두고 커밋하지 않은 TASK-WHY-01(서비스 WHY 정본)·TASK-BENCH-01(벤치마킹 자료) ② TASK-WEB-UI-02 전체(브랜드·캐릭터·조약돌 개편, 피드백 13건과 추가 요청, D-048~D-089, 작업방식 D-087). 넣지 않은 것: `.playwright-cli/`·`output/`(도구 임시 산출물).
- 사용자 추가 지적(2026-09-25): 오늘 화면 '지난 기록 ›'(D-076의 유일한 입구)도 대충 만들었다 — 회색 글자+가는 꺾쇠의 기본 링크 모양. HANDOFF-CAL-POLISH.md에 3번 항목과 원인 후보(기본 링크 모양·세계관 사물 없음·약한 무게·부품 말투 불일치·날짜와 한 덩어리)를 더했다. 새 세션이 함께 분석한다.
- QA #50 착수(2026-09-25, 새 세션): HANDOFF-CAL-POLISH 프롬프트대로 원인 분석 → 가설 판정 → 세 안 질문 → **D-090**(①④ 위 QA #50 행). 가설 판정 요약: 윗줄 — 리듬 없음·부품 넷·알약 무게는 맞음, 정렬선 셋은 부분(실제는 윗줄 폭이 카드보다 넓은 공통 영역 문제 + 왼쪽 열 원 셋 기둥). 종이 — 기울기·빈 줄·은유 섞임·머리 여백 맞음, 테이프는 대체로 틀림, 새로 찾음: 글자가 줄 위가 아니라 두 줄 사이 가운데에 뜸. 입구 — 날짜와 같은 색·크기·굵기라 날짜 꼬리로 읽힘(핵심), 기본 링크 모양·부품 말투 맞음, 대비는 11.3:1이라 '흐림'은 틀림. `<SP5>` = 세션 scratchpad `5a7a5d10-…/scratchpad`(캡처 도구 `tools/shoot.mjs`, 기준 캡처 `base/`). 구현은 §10대로 작업자 둘(달력: tabs.js renderCalendar·tabs-mood.css / 오늘: today.js·scene.css)에게 나눠 맡긴다.
- QA #50 통과(2026-09-25): 작업자 Sub-E(달력 — tabs.js renderCalendar·tabs-mood.css)·Sub-F(오늘 — today.js·scene.css), Sonnet 병렬. 메인 재측정(375×812): ① 제목→주 12·주→윗줄 16·윗줄→편지 16px, ×·크게 보기 보이는 36px/누르는 44px, 예시 태그–날짜 8px, 스크롤 0(360×740 이하는 짧게 스크롤 — 전에도 스크롤됐다). ② 네 상태 transform none·빈 줄 0·글자 기준선이 선 위 4~5px·조약돌 밑면 2px·종이 위 끝→첫 글자 약 26px, 모든 버튼이 같은 잉크 알약(40/44)이고 버튼 줄은 줄 없는 여백, 1280×900에서도 늘어나지 않음, 360×640은 있었던 일 첫 줄을 남기고 23px 스크롤, 날짜 색 subtle→muted(4.35→6.12:1). 펼치기·닫기 초점, 종이 클릭, 뒤로 가기, 2단계 삭제 시트, 스크립트 오류 0(헤드리스 클릭 재현). ③ 알약 36/44px, 오른쪽 끝 = 줄 끝, 세로 가운데 = 날짜 가운데(0px), 날짜 줄 24px·'오늘은 어떤' y 163.5 = 수정 전, 흰 글자 대비 13.85(11%)·11.67(호버 16%):1, 흐림·테두리·밑줄 없음. 새 맥락 verifier 마감 검토 1회: 결함 360×640 회귀·글자 앉힘 부족·문서 옛 표기 3곳·증거 미기록 → 모두 처리. 선택 사항 중 반영: 낡은 주석, 집 아이콘을 하단 탐색 경로 그대로, 날짜 대비. 남긴 것(이번 범위 밖, 기존 문제): scheduleFit이 날짜를 고를 때마다 resize·ResizeObserver를 새로 붙임, 2단계 삭제 뒤 초점이 body로 떨어짐. `check-contrast` 349건·`quick`·`full` 28/28 PASS. 전/후 캡처 `<SP5>/final/before-after-1-3.png`·`before-after-2.png`. 문서: D-090(D-076·D-088 상태 칸 갱신 표시), UX_SPEC §1·오늘·달력 절, DESIGN_SYSTEM 달력 부품·§6.11 입구·§6.12·대비 절, `scripts/check-contrast.mjs` 입구 알약 항목.
- QA #50 추가 지적(2026-09-25, 사용자 캡처): 고른 날 고리가 이웃 날짜와 겹침, 월 제목과 ‹ 버튼이 너무 가까움. 실측 — 조약돌 inset 2px+오프셋 3px+두께 2px로 고리가 칸 밖 3px까지 나가 이웃 조약돌을 1px 덮음(한 달·접힌 주 모두), ‹ 원 끝→제목 8px. 고침(메인 직접, 두 줄): tabs.css 고른 날 조약돌 inset 5px·오프셋 2px(고리 바깥 끝 칸 안 1px, 이웃과 3px), tabs-mood.css 제목 간격 8→16px. 375·360 접힌 주·한 달·오늘 칸 선택 캡처 확인, 가로 넘침 0. 캡처 `<SP5>/ring/zoom.png`. 미리보기 서버가 꺼져 있어 launch `web-prototype`으로 다시 띄웠다.
- QA #50 추가 요청(2026-09-25): '달력 클릭할 때마다 달력 새로 나오는 애니메이션 없애고, 클릭한 날짜가 살짝 흔들리게' → 이해한 내용을 먼저 말하고 확인받음(처음 들어올 때 한 번은 남김·달 넘길 때도 없앰·결은 흔들림). 원인: 날짜를 누르면 paintGrid가 한 달 격자를 새로 만들어 tb-pop이 매번 다시 재생됐다. 고침(메인 직접): paintGrid 두 번째부터 `.cal-settled`로 tb-pop을 끄고, selectDate가 누른 칸에 `.cal-wiggle`(420ms, rotate −5°→4°→−2°). 헤드리스 확인(움직임 켬): 처음 진입 tb-pop, 날짜 클릭 뒤 다른 칸 animation none·누른 칸 cal-wiggle(90ms에 −4.7°), ‹ 달 넘김 none·opacity 1, 펼침 닫기 뒤 none. 흔들리는 프레임에서도 고리가 이웃과 안 겹침(`<SP5>/wig/z.png`). 움직임 줄이기는 기존 reduce 규칙(.calendar .cal .day animation:none)이 함께 끈다.
- QA #50 추가 지적(2026-09-25): '선택된 날짜가 잘 안 보인다' → 원인: 고른 날 표시가 완료 칸과 같은 ink라 ink 조약돌 사이에서 '조금 큰 ink 조약돌'로만 읽힘(고유한 시각 채널 없음) + 겹침 수정으로 조약돌이 줄어 더 약해짐. 세 안 캡처(`<SP5>/sel/compare-rows.png`: A 굵은 ink 고리 / B 나머지 32%로 옅게 — 누를 수 없는 날처럼 보임 / C 민트 띠 + ink 고리) → 사용자 C(추천) 선택. 고침(메인): tabs.css 고른 날 ::before inset 7px + box-shadow 민트 4px spread + ink 2px outline(오프셋 4px), 바깥 끝 칸 안 1px·이웃과 3px. 완료·빈 날·오늘·접힌 주·360 확인(`<SP5>/selv/zoom.png`).
