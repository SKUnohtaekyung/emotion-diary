{
  "name": "emotion-diary",
  "version": "0.0.0",
  "private": true,
  "description": "모바일 우선 개인용 감정일기 MVP",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "characters:build": "node scripts/build-character-animations.mjs",
    "verify": "node scripts/verify.mjs --mode full",
    "verify:quick": "node scripts/verify.mjs --mode quick",
    "verify:full": "node scripts/verify.mjs --mode full",
    "test": "node scripts/test-check-taxonomy.mjs && node scripts/test-claude-scope-guard.mjs && node scripts/test-claude-git-guard.mjs && node scripts/test-check-characters.mjs && node scripts/test-check-harness.mjs"
  },
  "devDependencies": {
    "sharp": "^0.35.4"
  }
}
