# 감정일기 — MVP 제품 요구사항

## 1. 문서의 역할

이 문서는 제품이 **무엇을 제공해야 하는지**와 사용자가 어떤 상태를 완료로 느껴야 하는지를 정하는 제품 정본이다. 구현 방식은 [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md), 데이터 불변조건은 [docs/DATA_MODEL.md](docs/DATA_MODEL.md), 화면은 [docs/UX_SPEC.md](docs/UX_SPEC.md), AI는 [docs/AI_RAG_SPEC.md](docs/AI_RAG_SPEC.md), 시험은 [docs/EVAL_PLAN.md](docs/EVAL_PLAN.md)를 따른다.

이 패키지를 새 프로젝트에 넣을 때 [BOOTSTRAP.md](BOOTSTRAP.md)의 기술 감사를 먼저 수행한다. 제품 핵심은 임의로 바꾸지 않되, 검증에 실패한 기술 가설은 [docs/DECISIONS.md](docs/DECISIONS.md)의 절차로 교정한다.

## 2. 제품 정의와 경계

감정일기는 사용자가 매일 사건, 감정 단어, 강도와 이유를 기록하고, 기록이 쌓이면 자신의 감정 패턴을 근거와 함께 돌아보게 돕는 개인용 모바일 우선 웹앱이다.

목적은 감정을 더 정확한 말로 알아차리고 기록 습관을 만들며, **개인 기록 안에서 관찰되는 변화**를 성찰하게 하는 것이다. 의료·심리 진단, 치료, 성격/애착유형 판정, 위기 대응 서비스가 아니다. 민감한 의료기록 또는 PHI를 입력하는 용도로 제공하지 않는다.

### MVP 성공 상태

제작자 한 명이 휴대폰에서 제한된 사이트에 접근해 직접 또는 AI 대화로 한 편의 일기를 작성하고, 명시적으로 완료하고, 같은 날짜 기록을 다시 열어 수정·삭제·내보내며, 누적 기록의 재현 가능한 대시보드와 근거가 충분한 경우의 제한적 AI 분석을 볼 수 있다. AI나 RAG가 실패해도 직접 작성·저장·결정론적 대시보드는 동작한다.

## 3. 요구사항 레지스트리

상세 의미는 아래 절이 정본이며, 테스트 연결은 [docs/TRACEABILITY.md](docs/TRACEABILITY.md)에 둔다.

| ID | 필수 결과 |
| --- | --- |
| PR-001 | 자기성찰 도구의 비진단·비치료 경계를 지킨다 |
| PR-002 | 제한된 접근(본인 PC 자체 호스팅+무료 사설망), 개인 저장, 모바일 브라우저 사용을 제공한다 |
| PR-003 | 사용자 첨부 자료의 원형 일기 6영역과 순서를 보존한다 |
| PR-004 | 7개 상위 감정과 첨부 이미지의 모든 세부 감정을 버전·출처와 함께 보존한다 |
| PR-005 | 상위·세부 감정을 복수 선택하고 각 세부 감정의 강도 1~10을 저장한다 |
| PR-006 | 직접 작성·검토·완료 흐름을 제공한다 |
| PR-007 | AI 대화 작성이 사용자 발화만 구조화하고 사용자 승인 뒤 완료한다 |
| PR-008 | owner의 현지 날짜당 한 기록, 소급 작성, 실제 작성일 기반 streak를 보장한다 |
| PR-009 | 구조화 draft 자동저장, 완료조건, 수정과 분석 stale 처리를 제공한다 |
| PR-010 | 감정별 색상·캐릭터를 접근 가능하고 비진단적으로 사용한다 |
| PR-011 | 시간·ON/OFF·완료 상태를 반영한 알림과 미지원 시 인앱 fallback을 제공한다 |
| PR-012 | 완료 기록만으로 기간별 결정론적 대시보드를 제공한다 |
| PR-013 | Evidence Store/Card/Gate/Verifier를 통과한 분석만 표시한다 |
| PR-014 | Diary/Evidence 분리, 서버 권한, 최소 전송, hard delete, JSON export를 제공한다 |
| PR-015 | 모바일·접근성·오류/fallback과 명시된 MVP 제외 범위를 지킨다 |

## 4. 사용자·접근·배포 전제

- 초기 사용자는 제작자 본인 한 명이다. 저장 구조는 항상 `owner` namespace를 가진다.
- 사용자는 제한된 접근 경계를 통과한 뒤에만 일기·대시보드·설정을 사용할 수 있다. 클라이언트가 owner를 선택할 수 없다.
- 배포는 **제작자 본인 PC의 자체 호스팅**이다(D-030, 2026-09-02 승인). 서버와 SQLite 데이터 파일은 본인 PC에 있고, 휴대폰은 Tailscale 같은 무료 사설망을 통해서만 접속한다. 공개 인터넷에 노출하지 않는다.
- 사설망 접속 제한과 서버 측 접근 토큰이 MVP의 접근 경계로 충분한지, 로컬 서버·SQLite·비밀값·백업이 민감 일기 요구를 충족하는지는 부트스트랩 스파이크에서 실제로 검증한다.
- 검증이 실패하면 공개 배포나 불완전한 자체 로그인을 덧붙이지 않고 무료 범위의 다른 비공개 hosting/backend를 선택한다(D-021).
- AI 기능은 유료 API 없이 본인 PC의 Claude 구독 경로로만 제공하며, 본인 1명 전용이다(D-019, D-029).
- 인터넷 전체 공개는 MVP가 아니다. 공개 또는 다중 사용자 전환에는 새 인증·row isolation·개인정보 위협 모델과 사용자 승인이 필요하다.
- 휴대폰 브라우저와 홈 화면 바로가기를 1차 사용 방식으로 한다. standalone PWA, service worker, Web Push는 실제 지원과 데이터 경계를 검증한 경우에만 제공한다.

## 5. 사용자 원형 감정일기

[references/source/journal-template.txt](references/source/journal-template.txt)의 다음 순서와 의미가 공식 원형이다. 화면이 단계형/한 화면인지와 무관하게 저장·조회·AI 초안·내보내기에서 이 구조를 잃지 않는다.

1. 기록일자
2. 일어난 사건
3. 내가 느낀 감정 + 강도(1~10)
4. 왜 그런 감정이 들었을까
5. 칭찬할 점 세 가지
6. 감사할 점 세 가지

완료 필수는 `사건`, 세부 감정 1개 이상, 선택한 **각** 세부 감정의 강도, `이유`다. 칭찬과 감사는 각각 세 입력 슬롯을 항상 제공하되 MVP 완료에는 선택사항이다. 사용자가 말하지 않은 빈 슬롯을 AI가 생성해 채우지 않는다.

## 6. 감정 체계와 시각 표현

### 6.1 공식 분류

사용자가 첨부한 두 감정 단어 이미지가 세부 감정 한글 표기와 소속의 원자료다.

| 상위 감정 | 원자료의 시각 계열 |
| --- | --- |
| 즐거움 | 연두·초록, 춤추는 흰 공룡/고양이 |
| 바램 | 연보라, 고양이 |
| 슬픔 | 하늘·청색, 물 위의 고양이 |
| 분노 | 주황·붉은색, 폭발/어두운 고양이 |
| 기쁨 | 노랑, 날아가는 고양이 |
| 사랑 | 분홍, 하트 고양이 |
| 미움 | 남색·보라, 흰 공룡 |

구현은 두 이미지의 모든 세부 단어를 임의로 축소·개명·이동하지 않는다. 원본을 변경하지 않고 versioned taxonomy로 전사하며, OCR 결과는 원본 이미지와 사람 또는 사용자 검수를 통과해야 한다.

### 6.2 선택과 강도

- 한 일기에서 상위 감정과 세부 감정을 복수 선택할 수 있다.
- 최종 저장 단위는 세부 감정이며, 각 세부 감정에 독립된 정수 강도 1~10을 둔다.
- 같은 세부 감정을 한 일기에 중복 저장하지 않는다.
- 강도 옆에 다음 앵커를 보여 준다.
  - 1: 거의 스쳐 지나간 감정
  - 5: 분명히 느껴졌고 하루에 영향을 준 감정
  - 10: 매우 커서 일상 행동에 큰 영향을 준 감정
- AI는 강도를 추정·판정하지 않고 사용자가 직접 선택하도록 질문한다.

### 6.3 색상과 캐릭터

- 흰색/중립 배경의 간결한 모바일 UI를 기본으로 한다.
- 선택 감정의 카테고리 색을 부드러운 강조·태그·카드로 사용한다.
- 해당 캐릭터 이미지를 선택·기록·회고에 일관되게 사용한다.
- 색과 캐릭터는 감정의 좋고 나쁨, 위험, 진단 점수를 뜻하지 않는다.
- 복수 감정은 배경색 혼합보다 독립 label/card를 사용하고 색 외의 텍스트·아이콘을 함께 둔다.

## 7. 핵심 사용자 흐름

### 7.1 시작

접근 확인 뒤 오늘 화면에서 다음 두 경로를 제공한다.

1. **직접 작성** — 기본이자 우선 구현 경로
2. **AI와 대화하며 작성** — 같은 구조로 들어가는 보조 경로

오늘 record가 있으면 새 record가 아니라 이어쓰기/보기·수정을 연다.

### 7.2 직접 작성

`날짜 → 사건 → 상위/세부 감정 복수 선택 → 각 강도 → 이유 → 칭찬 3칸 → 감사 3칸 → 전체 검토 → 완료`

모든 단계로 돌아가 수정할 수 있고 입력은 구조화 draft로 자동 임시저장된다. 저장 중·성공·실패·revision 충돌을 사용자에게 표시하며 실패로 입력을 지우지 않는다.

### 7.3 AI 대화 작성

`사건을 열린 질문으로 탐색 → 필요한 감정 후보 제안 → 사용자가 선택/추가/삭제 → 사용자가 각 강도 선택 → 이유 질문 → 칭찬/감사 선택 질문 → 구조화 초안 → 사용자 검토·수정 → 완료`

- AI는 한 번에 한 가지를 묻고 비판단적으로 짧게 응답한다.
- “답답함이나 서운함에 가까웠을까요? 다른 표현도 괜찮아요”는 허용하지만 “당신은 서운합니다”는 금지다.
- 사건·인물·감정·원인·강도·칭찬·감사를 창작하거나 확정하지 않는다.
- 전체 대화 transcript는 기본 영구 저장하지 않고 구조화 draft만 복구한다.
- AI 실패/timeout/보류 시 현재 draft를 유지하고 즉시 직접 작성으로 이어갈 수 있다.

## 8. 날짜·상태·완료 규칙

- 서버가 확인한 owner의 현지 `entry_date`당 draft/completed를 합쳐 하나다.
- 미래 날짜는 허용하지 않고 과거 날짜는 허용한다.
- `entry_date`, 실제 최초 완료 시각/현지 날짜, 마지막 수정 시각을 구분한다.
- 소급 record는 해당 `entry_date`의 차트·분석에는 포함하지만 이미 끊긴 streak를 복구하지 않는다.
- streak는 실제 `first_completed_local_date`의 연속성으로 계산한다.
- completed만 streak, 정식 통계, AI 분석 입력에 포함한다.
- completed record도 수정 가능하다. revision이나 통계 snapshot이 바뀌면 기존 분석을 `stale`로 표시한다.
- 삭제는 복구 불가를 확인한 뒤 종속 데이터를 영구 삭제한다. 내보내기는 schema version이 있는 UTF-8 JSON이다.

세부 상태·충돌·보존 계약은 [docs/DATA_MODEL.md](docs/DATA_MODEL.md)가 정본이다.

## 9. MVP 기능

| 영역 | 제품 결과 |
| --- | --- |
| 접근·저장 | 제한 접근, owner 격리, 구조화 draft autosave, 조회·수정·hard delete, JSON export |
| 직접 기록 | 원형 구조, taxonomy, 복수 감정, 감정별 강도, 소급 작성, 검토·완료 |
| AI 기록 | 사용자 발화만 질문·구조화, 최종 사용자 선택·승인, 직접 작성 fallback |
| 기록 탐색 | 날짜별 없음/draft/completed, 상세와 수정 |
| 대시보드 | 7/30일 또는 기간별 추세, 카테고리 비중, 세부 감정 빈도, 평균+n, streak |
| 알림 | 기본 제안 오후 10시, 사용자 시간·ON/OFF·timezone, 오늘 completed 뒤 중단, 인앱 fallback |
| 표현 | 감정별 색과 캐릭터, 색에만 의존하지 않는 접근성 |
| 분석 | 관찰·근거·가능한 맥락·한계·성찰 질문의 분리, 근거 실패 시 정상 보류 |

소급 작성은 오늘의 알림 완료 여부를 바꾸지 않는다. SMS·카카오톡·이메일 알림은 MVP가 아니다.

## 10. 대시보드와 근거 기반 분석

대시보드는 완료 record의 구조화 데이터만 순수 코드/SQL로 계산한다. 기록 없는 날짜는 강도 0이 아니라 missing이며, 평균에는 표본 수와 기간/분모를 표시한다.

AI 주간/기간 분석은 다음 순서로 읽히게 한다.

1. 개인 기록에서 계산된 관찰
2. 표시 가능한 diary/stat 근거
3. Evidence Gate를 통과한 경우에만 일반적 심리학 맥락
4. 데이터·연구의 한계
5. 진단이 아닌 열린 성찰 질문

“최근 7일에 높은 강도의 답답함이 반복 기록되었습니다” 같은 관찰은 가능하다. 우울증·불안장애·애착유형·성격을 진단/확률화하거나 치료·복약·위험행동을 지시하지 않는다. RAG는 [docs/AI_RAG_SPEC.md](docs/AI_RAG_SPEC.md), 위기·개인정보는 [docs/SAFETY_POLICY.md](docs/SAFETY_POLICY.md)를 따른다.

## 11. 데이터와 개인정보 요구

- Diary Store와 Psychology Evidence Store를 분리하고 일기 원문을 Evidence Vector Store에 넣지 않는다.
- 모든 읽기·쓰기·분석·삭제·export에서 server-derived owner 권한을 검사한다.
- API key와 비밀값은 서버 전용이며 브라우저·저장소·로그에 노출하지 않는다.
- 분석에는 결정론적 최소 통계와 필요한 제한 발췌만 전송한다.
- 일기·통계·AI 결과·Evidence·모델/프롬프트/schema version을 구분한다.
- 실제 공급자 보존·삭제·residency를 확인할 수 없으면 보장한다고 표현하지 않는다.
- 위기 신호는 일반 분석을 중단하고 [docs/SAFETY_POLICY.md](docs/SAFETY_POLICY.md)의 안전 흐름으로 전환한다.

## 12. MVP 제외와 향후 후보

MVP 제외:

- STT 음성 입력과 실시간 음성 대화
- SMS·카카오톡·이메일 알림
- 진단·치료·성격/애착 판정과 자동 위기 신고
- 고급 장기·월간 분석과 검증 심리 척도
- 지식 그래프, 자체 embedding, hybrid search, reranker
- native app/WebView wrapper, 검증되지 않은 offline/PWA/Web Push
- 다중 사용자 공개 서비스, CSV/import

향후에는 `음성 입력 → STT → 일반 AI 대화`를 실시간 음성과 분리해 검토하고, 지원이 확인된 PWA/push, 장기 분석, 검증 척도, 지식 그래프/고급 검색, native wrapper를 평가할 수 있다. 편입 전 PRD·Decision·평가 기준과 개인정보 영향 검토가 먼저다.

## 13. 제품 완료·출시 게이트

MVP는 다음을 모두 만족해야 한다.

- [docs/EVAL_PLAN.md](docs/EVAL_PLAN.md)의 기능·모바일·접근성·데이터·AI·RAG·안전 게이트 통과
- 실제 배포의 접근·저장·비밀값·보존·삭제 경계 확인
- 사용자 원자료 taxonomy의 완전 전사와 검수
- Web Push 등 미지원 항목의 정확한 보류/fallback 표시
- 차단/높음 결함 0, [harness/quality-gates.yaml](harness/quality-gates.yaml)의 full/release 증거

RAG가 미달이면 분석 기능만 제외하고 직접 작성·결정론적 대시보드의 제한 MVP를 별도 사용자 승인 후 배포할 수 있다. 개인정보·권한 경계가 미확인이면 어떤 형태로도 배포하지 않는다.

