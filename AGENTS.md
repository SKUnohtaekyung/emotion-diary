# Agent Operating Contract

이 파일은 Claude Code와 Codex가 공유하는 저장소 작업 계약이다. 제품 세부사항은 해당 정본 문서를 읽고, 이 파일에는 모든 작업에 항상 필요한 규칙만 둔다.

**규칙은 두 에이전트 공통이지만 기계적 강제는 공통이 아니다.** §2.1·§2.2·§5의 훅(`.claude/settings.json`)은 Claude Code에만 걸려 있고, Codex 세션에는 같은 규칙이 문서로만 적용된다(2026-09-19의 Codex 커밋 `98bbe4e`는 §2.1의 메시지 규칙을 따르지 않았다). 도구와 무관하게 걸리는 장치는 GitHub Actions(`npm ci` → quick → full, Ubuntu·Windows)뿐이다. Codex 쪽 대응 수단과 그 한계는 §2.3에 적는다.

## 1. 작업 시작

**세션을 시작하면 가장 먼저 [docs/SERVICE_WHY.md](docs/SERVICE_WHY.md)를 끝까지 읽는다.** 이 서비스의 WHY — 서비스를 만들게 된 핵심 이유 — 이고, 모든 제품·디자인·AI·문구 판단이 기대는 기준이다. 특히 §17 제품 원칙, §19 해석 범위와 과대해석 금지(외부 발표·PRD·투자 자료에서 근거를 쓸 때), §20 마지막 기준(어떤 기능이든 통과해야 할 질문)을 작업 내내 적용한다. 판단이 이 문서와 어긋나면 추측으로 정하지 말고 아래 5번처럼 차단 사항으로 기록해 사용자에게 묻는다. Claude Code는 `CLAUDE.md`의 import로 세션 시작 때 자동으로 불러오고, Codex와 그 밖의 도구는 직접 읽는다. 받은 원본은 `references/source/why/`에 해시로 고정해 보존한다(`references/manifest.json`).

1. `tasks/CURRENT_TASK.md`에서 승인 범위, 완료 조건, 소유 파일, 차단 사항을 확인한다.
2. `README.md`의 정본 지도를 따라 작업과 직접 관련된 문서만 읽는다.
3. 기존 파일·변경 상태·테스트를 먼저 확인하고, 사용자의 미완료 변경을 보존한다.
4. 요구사항 ID와 검증 항목을 `docs/TRACEABILITY.md`에서 연결한다.
5. 모호함이 안전·개인정보·데이터 손실·제품 범위를 바꾸면 추측하지 말고 차단 사항으로 기록해 사용자에게 묻는다.

### 코드 구조 탐색

- symbol 위치·caller/callee·dependency·reference·변경 영향은 Codebase Memory Graph로 후보를 좁힌 뒤 현재 working tree의 source와 diff를 직접 확인한다. Graph는 탐색 보조이며 정본이 아니다.
- 세션 첫 사용에 `list_projects`/`index_status`로 공유 index를 확인한다. 각 Agent가 전체 재인덱싱하지 않는다. Graph가 없거나 오래됐거나 parser·동적/문자열 연결을 다루지 못하면 targeted Grep/Glob → 관련 Read로 전환한다.
- 단순 국소 수정에 불필요한 Graph 호출을 강제하지 않는다. Graph만으로 부재·dead code·호환성을 판단하거나 source 확인 없이 rename/dependency 삭제를 하지 않는다.
- 탐색 깊이·역할·인덱싱/복구 절차는 `docs/AGENT_WORKFLOW.md` §9를 따른다.

## 2. 작업 단위와 소유권

- 한 작업은 하나의 `CURRENT_TASK`와 명시된 인수 조건을 가진다.
- 한 파일에는 한 명의 writer만 둔다. **진짜 동시에(같은 시각에 병렬 프로세스로) 실행되는** writer는 서로 다른 worktree/branch와 비중복 소유 파일을 사용한다. 순차로 배치되는 하위 에이전트(현재 일반적인 방식)는 별도 worktree를 요구하지 않는다 — 각자에게 명시한 소유 파일과 §2.2의 기계적 확인 훅으로 대응한다(근거: D-042, 실제로 worktree가 쓰인 적이 없고 이슈 #27의 사고도 순차 배치 사이에서 났다).
- 조사자와 검증자는 기본적으로 읽기 전용이다. 검증자가 발견한 문제를 직접 고치지 않고 writer에게 재현 가능한 증거로 돌려준다.
- 다른 에이전트나 사용자의 변경을 되돌리거나 전체 파일을 이유 없이 재작성하지 않는다.
- 실험적 Claude agent teams나 자율 다중 에이전트는 기본 경로가 아니다. 격리 가능하고 소유권이 명확할 때만 사용한다.
- 쓰기 작업자를 여럿 병렬로 두는 큰 작업은 `docs/AGENT_WORKFLOW.md` §10을 따른다(D-087). 기준이 커밋돼 있으면 작업자마다 worktree를 쓴다. 커밋 전 변경 위에서 일해야 하면 같은 폴더에서 소유 파일을 나눠 쓴다. 두 경우 모두 메인이 결과를 독립적으로 다시 재고, 같은 작업의 재작업은 두 번까지다. 마감 검토는 새 맥락의 리뷰어가 한다.

### 2.1 커밋 권한

- **주 에이전트는 사용자가 명시적으로 지시할 때만 커밋한다.** GitHub 작업(커밋·푸시·PR)은 지시받으면 수행한다.
- **하위 에이전트는 어떤 경우에도 커밋하지 않는다.** `git commit`·`git add`·`git push`를 실행하지 않고 작업 결과만 보고한다. 사용자 승인은 주 에이전트에게 온 것이고, 하위 에이전트의 커밋은 사용자 눈에 보이지 않는다.
- 이 규칙은 `.claude/settings.json`의 `PreToolUse` 훅(`scripts/claude-git-guard.mjs`)이 강제한다 — 커밋·푸시로 보이는 Bash 명령은 사용자 확인을 거친다. 차단이 아니라 확인이므로 지시받은 작업은 그대로 진행된다. **권한 모드가 auto인 세션에서는 주 에이전트의 커밋·푸시에 확인창을 띄우지 않는다**(D-060) — auto를 켠 것이 자동 진행에 대한 사용자의 위임이고, 훅의 확인은 auto 모드도 막지 못해 작업마다 창이 떴다. 위 두 규칙은 그대로이며 **하위 에이전트의 커밋·푸시는 auto에서도 확인한다.** 훅이 모드를 알 수 없으면(입력에 `permission_mode`가 없으면) 종전대로 확인한다.
- 근거: 2026-09-05 taxonomy 작업에서 하위 에이전트가 프롬프트의 커밋 금지를 어기고 커밋해 되돌려야 했다(`docs/PROCESS_LOG.md`). 프롬프트에 적은 금지는 지켜지지 않았고 기계 검사만 지켜졌다.
- **커밋 메시지**: 한국어로 쓰고 "무엇을"보다 "왜"를 적는다. 마지막 줄은 `Co-Authored-By: <그 커밋을 실제로 만든 에이전트·모델> <noreply 주소>`다 — 예: `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. 모델 이름을 고정하지 않는다. 세션마다 모델이 다르므로 푸터가 서로 다른 것이 정상이고, 푸터가 없는 에이전트 커밋이 위반이다. 이 규칙의 정본은 이 줄 하나다(2026-09-20 사용자 결정). 다른 문서에 복제하지 않는다.
- PR 병합·이슈 닫기처럼 `gh`로 하는 되돌리기 어려운 작업은 위 훅이 잡지 못한다(`scripts/test-claude-git-guard.mjs`의 known 미탐). 커밋과 똑같이 사용자 지시가 있을 때만 한다.

### 2.2 소유 파일 확인 훅

- `.claude/settings.json`의 `PreToolUse` 훅(matcher `Write|Edit`, `scripts/claude-scope-guard.mjs`)이 `tasks/CURRENT_TASK.md`의 `- 소유 파일: ...` 선언과 대조해, 목록 밖 경로에 쓰면 사용자 확인을 요구한다. 커밋 훅(§2.1)과 같은 방식(차단이 아니라 확인)이라 지시받은 작업은 그대로 진행된다.
- **확인하지 않는 경우**(D-060): 권한 모드가 auto일 때 — 이 검사는 꺼진다(소유 파일 밖 쓰기의 사후 점검은 이슈 #27이 제안한 세션 경계 스냅샷으로 남은 일이다). 그리고 대상이 Claude Code 자신의 작업 폴더(세션 scratchpad, 자동 메모리 폴더)일 때 — 저장소 파일이 아니므로 어느 모드에서나 제외한다. 선언 줄의 backtick 조각 하나가 정규식으로 바뀌지 않아도 훅은 죽지 않고 그 조각만 건너뛴다(2026-09-19~21에 설명문 속 `?theme=pebble` 조각으로 29회 죽어 검사가 조용히 꺼져 있었다).
- 새 작업을 시작하면 `CURRENT_TASK`에 `- 소유 파일: ...` 형식으로 먼저 선언한다(기존 섹션과 동일한 표기). 선언하지 않은 경로에 쓰면 auto가 아닌 모드에서 이 훅이 매번 확인을 요구한다. 선언 줄에는 경로·글롭만 backtick으로 싸고 설명은 backtick 없이 쓴다.
- 알려진 한계(과다 허용 쪽): 작업 상태와 무관하게 파일 전체의 모든 선언을 합치고, Bash를 통한 파일 쓰기는 matcher 밖이라 잡지 못한다. 첫 번째 한계는 끝난 작업의 절을 `tasks/archive/`로 옮기면 줄어든다(훅은 `CURRENT_TASK.md`만 읽는다). 상세는 `tasks/archive/TASK-ISSUE-27.md`.
- 근거: 이슈 #27 — 하위 에이전트 사이에 누구의 소유 파일도 아닌 `docs/PROCESS_LOG.md`에 출처 불명 변경이 생겼는데, §2의 소유 파일 규칙이 문서로만 있고 기계로 검사되지 않아 잡지 못했다(`docs/PROCESS_LOG.md`, D-042).

### 2.3 Codex 세션에서의 강제 수단(미구성)

2026-09-20 공식 문서 조사 결과다(`learn.chatgpt.com/docs/hooks`, `…/agent-configuration/rules`). **이 저장소의 Codex 런타임에서 검증하지 않았으므로 구성 전에 실제 동작을 확인한다**(§3의 마지막 규칙).

- Stop 훅은 동등하게 옮길 수 있다고 문서가 말한다(`decision: "block"` 또는 exit 2, `stop_hook_active` 제공). `.codex/hooks.json`은 `.gitignore` 대상이 아니라 커밋해 공유할 수 있다.
- `PreToolUse`는 `deny`만 되고 **`ask`(사용자 확인)를 지원하지 않는다.** `scripts/claude-git-guard.mjs`·`claude-scope-guard.mjs`를 Codex 훅에 그대로 연결하면 `ask` 출력이 무시되고 도구 호출이 그대로 진행된다 — 가드가 조용히 뚫리므로 그대로 연결하지 않는다.
- "확인 후 진행"에 가장 가까운 것은 `.codex/rules/`의 `prefix_rule(pattern=["git","commit"], decision="prompt")`인데 문서가 실험 기능이라고 밝힌다.
- 프로젝트 단위 훅·규칙은 그 프로젝트가 trusted일 때만 로드된다. 구성은 후속 작업이다.

## 3. 구현 규칙

- PRD에 없는 기능을 MVP에 임의 추가하지 않는다. 새 범위는 `docs/DECISIONS.md`에 결정 근거를 남긴 뒤 반영한다.
- 최소 범위를 수정하고 공개 계약·데이터 마이그레이션·보안 경계의 영향을 함께 검토한다.
- 사용자 ID, 권한, 날짜, 완료 상태, 분석 대상은 클라이언트 값을 신뢰하지 않고 서버에서 검증한다.
- 모든 저장 변경은 재시도에 안전해야 한다. 하루 1개 제약은 UI가 아니라 DB 불변조건으로 보장한다.
- 비밀값·개인 일기·실제 식별정보를 코드, 프롬프트 예제, 로그, 테스트 fixture, Evidence Store에 넣지 않는다.
- AI는 사용자 감정을 확정하거나 진단하지 않는다. 심리학적 해석은 `docs/AI_RAG_SPEC.md`의 Evidence Gate와 Verifier를 통과한 경우에만 표시한다.
- 외부 API·모델·Sites 기능은 구현 시점의 공식 문서와 실제 런타임으로 확인한다. 확인하지 않은 기능을 지원된다고 가정하지 않는다.

## 4. 상태와 문서 동기화

- 세션이 길어지거나 컨텍스트 압축이 예상되면 `CURRENT_TASK`의 체크포인트에 완료/다음/결정/실패 명령을 짧게 기록한다.
- 영구적인 제품·기술 판단은 채팅에만 남기지 않고 `docs/DECISIONS.md`에 기록한다.
- 데이터 또는 API 계약 변경은 `DATA_MODEL`, `ARCHITECTURE`, `EVAL_PLAN`, `TRACEABILITY` 중 영향을 받는 정본을 같은 변경에서 갱신한다.
- 사용자 제공 원자료는 `references/source`에 보존하고, 파생된 분류표에는 출처·버전·검수 상태를 남긴다.

## 5. 검증 루프

1. 변경 전 관련 실패 또는 현재 기준을 재현한다.
2. 가장 작은 변경을 구현한다.
3. 관련 테스트와 `node scripts/verify.mjs --mode quick`을 실행한다.
4. diff를 요구사항·개인정보·안전·오류 경로 관점에서 검토한다.
5. 완료 전 `node scripts/verify.mjs --mode full`을 실행하고 `CURRENT_TASK`에 증거를 남긴다.

같은 원인 가설로 두 번 실패하면 무한 반복하지 말고 가설을 폐기해 다시 진단한다. 일시적 도구/API 실패는 최대 두 번만 재시도한다. 권한 거부, 데이터 파괴 위험, 안전 정책 실패는 자동 재시도하지 않는다.

## 6. 완료 선언 조건

- 인수 조건이 전부 증거와 함께 충족됐다.
- 관련 테스트와 전체 하네스가 통과했다.
- 미해결 `차단` 또는 높은 심각도 결함이 없다.
- 문서·마이그레이션·추적성 표가 코드와 일치한다.
- 알려진 제한과 후속 작업이 `CURRENT_TASK` 또는 `ROADMAP`에 남아 있다.

테스트하지 않은 결과를 성공으로 보고하지 않는다. 실패나 미검증 항목은 그대로 명시한다.
